<?xml version="1.0" encoding="utf-8"?><!--
  ~ SPDX-FileCopyrightText: 2023 microG Project Team
  ~ SPDX-License-Identifier: Apache-2.0
  -->

<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <service android:name=".AdvertisingIdService"
                 android:exported="true">
            <intent-filter>
                <!-- AdvertisingIdClient binds this action literally (not via the
                     service broker), so it must stay on the un-renamed Google name. -->
                <action android:name="com.google.android.gms.ads.identifier.service.START" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </service>
    </application>
</manifest>
