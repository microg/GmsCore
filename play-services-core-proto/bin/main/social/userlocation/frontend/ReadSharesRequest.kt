// Code generated by Wire protocol buffer compiler, do not edit.
// Source: social.userlocation.frontend.ReadSharesRequest in locationsharingreporter/user_location_frontend_service.proto
@file:Suppress("DEPRECATION")

package social.userlocation.frontend

import com.squareup.wire.FieldEncoding
import com.squareup.wire.Message
import com.squareup.wire.ProtoAdapter
import com.squareup.wire.ProtoReader
import com.squareup.wire.ProtoWriter
import com.squareup.wire.ReverseProtoWriter
import com.squareup.wire.Syntax.PROTO_2
import com.squareup.wire.WireField
import com.squareup.wire.`internal`.JvmField
import com.squareup.wire.`internal`.JvmSynthetic
import kotlin.Any
import kotlin.Boolean
import kotlin.Int
import kotlin.Long
import kotlin.String
import kotlin.Suppress
import kotlin.Unit
import okio.ByteString

/**
 * Upload end
 */
public class ReadSharesRequest(
  @field:WireField(
    tag = 2,
    adapter = "social.userlocation.frontend.WearOsAccountAndLocationConfig#ADAPTER",
    schemaIndex = 0,
  )
  @JvmField
  public val config: WearOsAccountAndLocationConfig? = null,
  @field:WireField(
    tag = 4,
    adapter = "social.userlocation.frontend.GeofenceReportingConfig#ADAPTER",
    schemaIndex = 1,
  )
  @JvmField
  public val geofenceReportingConfig: GeofenceReportingConfig? = null,
  unknownFields: ByteString = ByteString.EMPTY,
) : Message<ReadSharesRequest, ReadSharesRequest.Builder>(ADAPTER, unknownFields) {
  override fun newBuilder(): Builder {
    val builder = Builder()
    builder.config = config
    builder.geofenceReportingConfig = geofenceReportingConfig
    builder.addUnknownFields(unknownFields)
    return builder
  }

  override fun equals(other: Any?): Boolean {
    if (other === this) return true
    if (other !is ReadSharesRequest) return false
    if (unknownFields != other.unknownFields) return false
    if (config != other.config) return false
    if (geofenceReportingConfig != other.geofenceReportingConfig) return false
    return true
  }

  override fun hashCode(): Int {
    var result = super.hashCode
    if (result == 0) {
      result = unknownFields.hashCode()
      result = result * 37 + (config?.hashCode() ?: 0)
      result = result * 37 + (geofenceReportingConfig?.hashCode() ?: 0)
      super.hashCode = result
    }
    return result
  }

  override fun toString(): String {
    val result = mutableListOf<String>()
    if (config != null) result += """config=$config"""
    if (geofenceReportingConfig != null) result +=
        """geofenceReportingConfig=$geofenceReportingConfig"""
    return result.joinToString(prefix = "ReadSharesRequest{", separator = ", ", postfix = "}")
  }

  public fun copy(
    config: WearOsAccountAndLocationConfig? = this.config,
    geofenceReportingConfig: GeofenceReportingConfig? = this.geofenceReportingConfig,
    unknownFields: ByteString = this.unknownFields,
  ): ReadSharesRequest = ReadSharesRequest(config, geofenceReportingConfig, unknownFields)

  public class Builder : Message.Builder<ReadSharesRequest, Builder>() {
    @JvmField
    public var config: WearOsAccountAndLocationConfig? = null

    @JvmField
    public var geofenceReportingConfig: GeofenceReportingConfig? = null

    public fun config(config: WearOsAccountAndLocationConfig?): Builder {
      this.config = config
      return this
    }

    public fun geofenceReportingConfig(geofenceReportingConfig: GeofenceReportingConfig?): Builder {
      this.geofenceReportingConfig = geofenceReportingConfig
      return this
    }

    override fun build(): ReadSharesRequest = ReadSharesRequest(
      config = config,
      geofenceReportingConfig = geofenceReportingConfig,
      unknownFields = buildUnknownFields()
    )
  }

  public companion object {
    @JvmField
    public val ADAPTER: ProtoAdapter<ReadSharesRequest> = object : ProtoAdapter<ReadSharesRequest>(
      FieldEncoding.LENGTH_DELIMITED, 
      ReadSharesRequest::class, 
      "type.googleapis.com/social.userlocation.frontend.ReadSharesRequest", 
      PROTO_2, 
      null, 
      "locationsharingreporter/user_location_frontend_service.proto"
    ) {
      override fun encodedSize(`value`: ReadSharesRequest): Int {
        var size = value.unknownFields.size
        size += WearOsAccountAndLocationConfig.ADAPTER.encodedSizeWithTag(2, value.config)
        size += GeofenceReportingConfig.ADAPTER.encodedSizeWithTag(4, value.geofenceReportingConfig)
        return size
      }

      override fun encode(writer: ProtoWriter, `value`: ReadSharesRequest) {
        WearOsAccountAndLocationConfig.ADAPTER.encodeWithTag(writer, 2, value.config)
        GeofenceReportingConfig.ADAPTER.encodeWithTag(writer, 4, value.geofenceReportingConfig)
        writer.writeBytes(value.unknownFields)
      }

      override fun encode(writer: ReverseProtoWriter, `value`: ReadSharesRequest) {
        writer.writeBytes(value.unknownFields)
        GeofenceReportingConfig.ADAPTER.encodeWithTag(writer, 4, value.geofenceReportingConfig)
        WearOsAccountAndLocationConfig.ADAPTER.encodeWithTag(writer, 2, value.config)
      }

      override fun decode(reader: ProtoReader): ReadSharesRequest {
        var config: WearOsAccountAndLocationConfig? = null
        var geofenceReportingConfig: GeofenceReportingConfig? = null
        val unknownFields = reader.forEachTag { tag ->
          when (tag) {
            2 -> config = WearOsAccountAndLocationConfig.ADAPTER.decode(reader)
            4 -> geofenceReportingConfig = GeofenceReportingConfig.ADAPTER.decode(reader)
            else -> reader.readUnknownField(tag)
          }
        }
        return ReadSharesRequest(
          config = config,
          geofenceReportingConfig = geofenceReportingConfig,
          unknownFields = unknownFields
        )
      }

      override fun redact(`value`: ReadSharesRequest): ReadSharesRequest = value.copy(
        config = value.config?.let(WearOsAccountAndLocationConfig.ADAPTER::redact),
        geofenceReportingConfig =
            value.geofenceReportingConfig?.let(GeofenceReportingConfig.ADAPTER::redact),
        unknownFields = ByteString.EMPTY
      )
    }

    private const val serialVersionUID: Long = 0L

    @JvmSynthetic
    public inline fun build(body: Builder.() -> Unit): ReadSharesRequest =
        Builder().apply(body).build()
  }
}
