// Code generated by Wire protocol buffer compiler, do not edit.
// Source: cryptauthv2.KeyDirective in cryptauth/cryptauth_directive.proto
@file:Suppress("DEPRECATION")

package cryptauthv2

import com.squareup.wire.FieldEncoding
import com.squareup.wire.Message
import com.squareup.wire.ProtoAdapter
import com.squareup.wire.ProtoReader
import com.squareup.wire.ProtoWriter
import com.squareup.wire.ReverseProtoWriter
import com.squareup.wire.Syntax.PROTO_3
import com.squareup.wire.WireField
import com.squareup.wire.`internal`.JvmField
import com.squareup.wire.`internal`.JvmSynthetic
import com.squareup.wire.`internal`.checkElementsNotNull
import com.squareup.wire.`internal`.immutableCopyOf
import com.squareup.wire.`internal`.sanitize
import kotlin.Any
import kotlin.Boolean
import kotlin.Int
import kotlin.Long
import kotlin.String
import kotlin.Suppress
import kotlin.Unit
import kotlin.collections.List
import okio.ByteString

/**
 * The policy to be handed down to the party which registered the public key
 * with the server. It is produced on the fly from current ServerSidePolicy
 * and PolicyConfig (PC).
 */
public class KeyDirective(
  /**
   * The specific policy which was used to generate this message.
   */
  @field:WireField(
    tag = 1,
    adapter = "cryptauthv2.PolicyReference#ADAPTER",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "policyReference",
    schemaIndex = 0,
  )
  @JvmField
  public val policy_reference: PolicyReference? = null,
  crossproof_key_names: List<String> = emptyList(),
  /**
   * The time when the key was enrolled/rotated (as observed by the server).
   * This should be the same as ServerSidePolicy.enroll_time_millis .
   */
  @field:WireField(
    tag = 3,
    adapter = "com.squareup.wire.ProtoAdapter#INT64",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "enrollTimeMillis",
    schemaIndex = 2,
  )
  @JvmField
  public val enroll_time_millis: Long = 0L,
  unknownFields: ByteString = ByteString.EMPTY,
) : Message<KeyDirective, KeyDirective.Builder>(ADAPTER, unknownFields) {
  /**
   * When rotating the current key, 'crossproof_key_name' keys should be used
   * to cross sign. This is retrieved from PolicyConfig.crossproof_key_name .
   */
  @field:WireField(
    tag = 2,
    adapter = "com.squareup.wire.ProtoAdapter#STRING",
    label = WireField.Label.REPEATED,
    jsonName = "crossproofKeyNames",
    schemaIndex = 1,
  )
  @JvmField
  public val crossproof_key_names: List<String> = immutableCopyOf("crossproof_key_names",
      crossproof_key_names)

  override fun newBuilder(): Builder {
    val builder = Builder()
    builder.policy_reference = policy_reference
    builder.crossproof_key_names = crossproof_key_names
    builder.enroll_time_millis = enroll_time_millis
    builder.addUnknownFields(unknownFields)
    return builder
  }

  override fun equals(other: Any?): Boolean {
    if (other === this) return true
    if (other !is KeyDirective) return false
    if (unknownFields != other.unknownFields) return false
    if (policy_reference != other.policy_reference) return false
    if (crossproof_key_names != other.crossproof_key_names) return false
    if (enroll_time_millis != other.enroll_time_millis) return false
    return true
  }

  override fun hashCode(): Int {
    var result = super.hashCode
    if (result == 0) {
      result = unknownFields.hashCode()
      result = result * 37 + (policy_reference?.hashCode() ?: 0)
      result = result * 37 + crossproof_key_names.hashCode()
      result = result * 37 + enroll_time_millis.hashCode()
      super.hashCode = result
    }
    return result
  }

  override fun toString(): String {
    val result = mutableListOf<String>()
    if (policy_reference != null) result += """policy_reference=$policy_reference"""
    if (crossproof_key_names.isNotEmpty()) result +=
        """crossproof_key_names=${sanitize(crossproof_key_names)}"""
    result += """enroll_time_millis=$enroll_time_millis"""
    return result.joinToString(prefix = "KeyDirective{", separator = ", ", postfix = "}")
  }

  public fun copy(
    policy_reference: PolicyReference? = this.policy_reference,
    crossproof_key_names: List<String> = this.crossproof_key_names,
    enroll_time_millis: Long = this.enroll_time_millis,
    unknownFields: ByteString = this.unknownFields,
  ): KeyDirective = KeyDirective(policy_reference, crossproof_key_names, enroll_time_millis,
      unknownFields)

  public class Builder : Message.Builder<KeyDirective, Builder>() {
    @JvmField
    public var policy_reference: PolicyReference? = null

    @JvmField
    public var crossproof_key_names: List<String> = emptyList()

    @JvmField
    public var enroll_time_millis: Long = 0L

    /**
     * The specific policy which was used to generate this message.
     */
    public fun policy_reference(policy_reference: PolicyReference?): Builder {
      this.policy_reference = policy_reference
      return this
    }

    /**
     * When rotating the current key, 'crossproof_key_name' keys should be used
     * to cross sign. This is retrieved from PolicyConfig.crossproof_key_name .
     */
    public fun crossproof_key_names(crossproof_key_names: List<String>): Builder {
      checkElementsNotNull(crossproof_key_names)
      this.crossproof_key_names = crossproof_key_names
      return this
    }

    /**
     * The time when the key was enrolled/rotated (as observed by the server).
     * This should be the same as ServerSidePolicy.enroll_time_millis .
     */
    public fun enroll_time_millis(enroll_time_millis: Long): Builder {
      this.enroll_time_millis = enroll_time_millis
      return this
    }

    override fun build(): KeyDirective = KeyDirective(
      policy_reference = policy_reference,
      crossproof_key_names = crossproof_key_names,
      enroll_time_millis = enroll_time_millis,
      unknownFields = buildUnknownFields()
    )
  }

  public companion object {
    @JvmField
    public val ADAPTER: ProtoAdapter<KeyDirective> = object : ProtoAdapter<KeyDirective>(
      FieldEncoding.LENGTH_DELIMITED, 
      KeyDirective::class, 
      "type.googleapis.com/cryptauthv2.KeyDirective", 
      PROTO_3, 
      null, 
      "cryptauth/cryptauth_directive.proto"
    ) {
      override fun encodedSize(`value`: KeyDirective): Int {
        var size = value.unknownFields.size
        if (value.policy_reference != null) size += PolicyReference.ADAPTER.encodedSizeWithTag(1,
            value.policy_reference)
        size += ProtoAdapter.STRING.asRepeated().encodedSizeWithTag(2, value.crossproof_key_names)
        if (value.enroll_time_millis != 0L) size += ProtoAdapter.INT64.encodedSizeWithTag(3,
            value.enroll_time_millis)
        return size
      }

      override fun encode(writer: ProtoWriter, `value`: KeyDirective) {
        if (value.policy_reference != null) PolicyReference.ADAPTER.encodeWithTag(writer, 1,
            value.policy_reference)
        ProtoAdapter.STRING.asRepeated().encodeWithTag(writer, 2, value.crossproof_key_names)
        if (value.enroll_time_millis != 0L) ProtoAdapter.INT64.encodeWithTag(writer, 3,
            value.enroll_time_millis)
        writer.writeBytes(value.unknownFields)
      }

      override fun encode(writer: ReverseProtoWriter, `value`: KeyDirective) {
        writer.writeBytes(value.unknownFields)
        if (value.enroll_time_millis != 0L) ProtoAdapter.INT64.encodeWithTag(writer, 3,
            value.enroll_time_millis)
        ProtoAdapter.STRING.asRepeated().encodeWithTag(writer, 2, value.crossproof_key_names)
        if (value.policy_reference != null) PolicyReference.ADAPTER.encodeWithTag(writer, 1,
            value.policy_reference)
      }

      override fun decode(reader: ProtoReader): KeyDirective {
        var policy_reference: PolicyReference? = null
        val crossproof_key_names = mutableListOf<String>()
        var enroll_time_millis: Long = 0L
        val unknownFields = reader.forEachTag { tag ->
          when (tag) {
            1 -> policy_reference = PolicyReference.ADAPTER.decode(reader)
            2 -> crossproof_key_names.add(ProtoAdapter.STRING.decode(reader))
            3 -> enroll_time_millis = ProtoAdapter.INT64.decode(reader)
            else -> reader.readUnknownField(tag)
          }
        }
        return KeyDirective(
          policy_reference = policy_reference,
          crossproof_key_names = crossproof_key_names,
          enroll_time_millis = enroll_time_millis,
          unknownFields = unknownFields
        )
      }

      override fun redact(`value`: KeyDirective): KeyDirective = value.copy(
        policy_reference = value.policy_reference?.let(PolicyReference.ADAPTER::redact),
        unknownFields = ByteString.EMPTY
      )
    }

    private const val serialVersionUID: Long = 0L

    @JvmSynthetic
    public inline fun build(body: Builder.() -> Unit): KeyDirective = Builder().apply(body).build()
  }
}
