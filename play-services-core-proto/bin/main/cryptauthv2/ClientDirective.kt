// Code generated by Wire protocol buffer compiler, do not edit.
// Source: cryptauthv2.ClientDirective in cryptauth/cryptauth_directive.proto
@file:Suppress("DEPRECATION")

package cryptauthv2

import com.squareup.wire.FieldEncoding
import com.squareup.wire.Message
import com.squareup.wire.ProtoAdapter
import com.squareup.wire.ProtoReader
import com.squareup.wire.ProtoWriter
import com.squareup.wire.ReverseProtoWriter
import com.squareup.wire.Syntax.PROTO_3
import com.squareup.wire.WireField
import com.squareup.wire.`internal`.JvmField
import com.squareup.wire.`internal`.JvmSynthetic
import com.squareup.wire.`internal`.checkElementsNotNull
import com.squareup.wire.`internal`.immutableCopyOf
import com.squareup.wire.`internal`.redactElements
import kotlin.Any
import kotlin.Boolean
import kotlin.Int
import kotlin.Long
import kotlin.String
import kotlin.Suppress
import kotlin.Unit
import kotlin.collections.List
import okio.ByteString

/**
 * This contains the directives handed down to the party which registered the
 * public key with the server. These directives are aggregated from across all
 * the policies of the keys that have been registered by this first party.
 */
public class ClientDirective(
  /**
   * The specific policy which was used to generate this message.
   */
  @field:WireField(
    tag = 1,
    adapter = "cryptauthv2.PolicyReference#ADAPTER",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "policyReference",
    schemaIndex = 0,
  )
  @JvmField
  public val policy_reference: PolicyReference? = null,
  /**
   * The first party should check in with the server after this period.
   * The server may require the client (first party) to rotate the key
   * (based on PolicyConfig.rotate_delay_millis from across all the policies
   * of the registered keys).
   * For each policy of a registered key a value is randomly drawn from
   * \[PC.checkin_delay_millis - PC.checkin_delay_millis_interval,
   * PC.checkin_delay_millis + PC.checkin_delay_millis_interval\].
   * The minimum value from across all these values is used.
   *
   * Whenever such a time period is coming due, the client should check in
   * all its keys with the server. The server tells the client which of those
   * keys need to be rotated and the rotation process proceeds for all these
   * keys (bundled together).
   */
  @field:WireField(
    tag = 2,
    adapter = "com.squareup.wire.ProtoAdapter#INT64",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "checkinDelayMillis",
    schemaIndex = 1,
  )
  @JvmField
  public val checkin_delay_millis: Long = 0L,
  /**
   * In case any call to CryptAuth v2 failed, the first party should retry
   * at most these many times right away, without the need to wait at all.
   * Passed in from PC.retry_attempts.
   * For example, a value of 1 means one original request, and if failed, a
   * single retry should follow.
   */
  @field:WireField(
    tag = 3,
    adapter = "com.squareup.wire.ProtoAdapter#INT32",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "retryAttempts",
    schemaIndex = 2,
  )
  @JvmField
  public val retry_attempts: Int = 0,
  /**
   * In case any call to CryptAuth v2 failed retry_attempts + 1 times, the first
   * party should retry the call again after this time period. If this latter
   * retry fails, the first party should wait this time period again then retry
   * and repeat until the request succeeds.
   * For each policy of a registered key a value is randomly drawn from
   * \[PC.retry_period_millis - PC.retry_period_millis_interval,
   * PC.retry_period_millis + PC.retry_period_millis_interval\].
   * The maximum value from across all these values is used.
   */
  @field:WireField(
    tag = 4,
    adapter = "com.squareup.wire.ProtoAdapter#INT64",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "retryPeriodMillis",
    schemaIndex = 3,
  )
  @JvmField
  public val retry_period_millis: Long = 0L,
  /**
   * The timestamp when this policy was minted.
   * This can help the client sync with the server's time.
   * checkin_delay_millis and retry_period_millis are relative to this time.
   * Without this timestamp, the client should act right away with regard to
   * the *_millis fields (ie, schedule something at NOW + *_millis).
   * With this timestamp (considering the times of both server and client are
   * in sync), the client would have all the required information for
   * a later action.
   */
  @field:WireField(
    tag = 5,
    adapter = "com.squareup.wire.ProtoAdapter#INT64",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "createTimeMillis",
    schemaIndex = 4,
  )
  @JvmField
  public val create_time_millis: Long = 0L,
  invoke_next: List<InvokeNext> = emptyList(),
  unknownFields: ByteString = ByteString.EMPTY,
) : Message<ClientDirective, ClientDirective.Builder>(ADAPTER, unknownFields) {
  /**
   * Which other services should be invoked after this interaction is complete.
   */
  @field:WireField(
    tag = 6,
    adapter = "cryptauthv2.InvokeNext#ADAPTER",
    label = WireField.Label.REPEATED,
    jsonName = "invokeNext",
    schemaIndex = 5,
  )
  @JvmField
  public val invoke_next: List<InvokeNext> = immutableCopyOf("invoke_next", invoke_next)

  override fun newBuilder(): Builder {
    val builder = Builder()
    builder.policy_reference = policy_reference
    builder.checkin_delay_millis = checkin_delay_millis
    builder.retry_attempts = retry_attempts
    builder.retry_period_millis = retry_period_millis
    builder.create_time_millis = create_time_millis
    builder.invoke_next = invoke_next
    builder.addUnknownFields(unknownFields)
    return builder
  }

  override fun equals(other: Any?): Boolean {
    if (other === this) return true
    if (other !is ClientDirective) return false
    if (unknownFields != other.unknownFields) return false
    if (policy_reference != other.policy_reference) return false
    if (checkin_delay_millis != other.checkin_delay_millis) return false
    if (retry_attempts != other.retry_attempts) return false
    if (retry_period_millis != other.retry_period_millis) return false
    if (create_time_millis != other.create_time_millis) return false
    if (invoke_next != other.invoke_next) return false
    return true
  }

  override fun hashCode(): Int {
    var result = super.hashCode
    if (result == 0) {
      result = unknownFields.hashCode()
      result = result * 37 + (policy_reference?.hashCode() ?: 0)
      result = result * 37 + checkin_delay_millis.hashCode()
      result = result * 37 + retry_attempts.hashCode()
      result = result * 37 + retry_period_millis.hashCode()
      result = result * 37 + create_time_millis.hashCode()
      result = result * 37 + invoke_next.hashCode()
      super.hashCode = result
    }
    return result
  }

  override fun toString(): String {
    val result = mutableListOf<String>()
    if (policy_reference != null) result += """policy_reference=$policy_reference"""
    result += """checkin_delay_millis=$checkin_delay_millis"""
    result += """retry_attempts=$retry_attempts"""
    result += """retry_period_millis=$retry_period_millis"""
    result += """create_time_millis=$create_time_millis"""
    if (invoke_next.isNotEmpty()) result += """invoke_next=$invoke_next"""
    return result.joinToString(prefix = "ClientDirective{", separator = ", ", postfix = "}")
  }

  public fun copy(
    policy_reference: PolicyReference? = this.policy_reference,
    checkin_delay_millis: Long = this.checkin_delay_millis,
    retry_attempts: Int = this.retry_attempts,
    retry_period_millis: Long = this.retry_period_millis,
    create_time_millis: Long = this.create_time_millis,
    invoke_next: List<InvokeNext> = this.invoke_next,
    unknownFields: ByteString = this.unknownFields,
  ): ClientDirective = ClientDirective(policy_reference, checkin_delay_millis, retry_attempts,
      retry_period_millis, create_time_millis, invoke_next, unknownFields)

  public class Builder : Message.Builder<ClientDirective, Builder>() {
    @JvmField
    public var policy_reference: PolicyReference? = null

    @JvmField
    public var checkin_delay_millis: Long = 0L

    @JvmField
    public var retry_attempts: Int = 0

    @JvmField
    public var retry_period_millis: Long = 0L

    @JvmField
    public var create_time_millis: Long = 0L

    @JvmField
    public var invoke_next: List<InvokeNext> = emptyList()

    /**
     * The specific policy which was used to generate this message.
     */
    public fun policy_reference(policy_reference: PolicyReference?): Builder {
      this.policy_reference = policy_reference
      return this
    }

    /**
     * The first party should check in with the server after this period.
     * The server may require the client (first party) to rotate the key
     * (based on PolicyConfig.rotate_delay_millis from across all the policies
     * of the registered keys).
     * For each policy of a registered key a value is randomly drawn from
     * \[PC.checkin_delay_millis - PC.checkin_delay_millis_interval,
     * PC.checkin_delay_millis + PC.checkin_delay_millis_interval\].
     * The minimum value from across all these values is used.
     *
     * Whenever such a time period is coming due, the client should check in
     * all its keys with the server. The server tells the client which of those
     * keys need to be rotated and the rotation process proceeds for all these
     * keys (bundled together).
     */
    public fun checkin_delay_millis(checkin_delay_millis: Long): Builder {
      this.checkin_delay_millis = checkin_delay_millis
      return this
    }

    /**
     * In case any call to CryptAuth v2 failed, the first party should retry
     * at most these many times right away, without the need to wait at all.
     * Passed in from PC.retry_attempts.
     * For example, a value of 1 means one original request, and if failed, a
     * single retry should follow.
     */
    public fun retry_attempts(retry_attempts: Int): Builder {
      this.retry_attempts = retry_attempts
      return this
    }

    /**
     * In case any call to CryptAuth v2 failed retry_attempts + 1 times, the first
     * party should retry the call again after this time period. If this latter
     * retry fails, the first party should wait this time period again then retry
     * and repeat until the request succeeds.
     * For each policy of a registered key a value is randomly drawn from
     * \[PC.retry_period_millis - PC.retry_period_millis_interval,
     * PC.retry_period_millis + PC.retry_period_millis_interval\].
     * The maximum value from across all these values is used.
     */
    public fun retry_period_millis(retry_period_millis: Long): Builder {
      this.retry_period_millis = retry_period_millis
      return this
    }

    /**
     * The timestamp when this policy was minted.
     * This can help the client sync with the server's time.
     * checkin_delay_millis and retry_period_millis are relative to this time.
     * Without this timestamp, the client should act right away with regard to
     * the *_millis fields (ie, schedule something at NOW + *_millis).
     * With this timestamp (considering the times of both server and client are
     * in sync), the client would have all the required information for
     * a later action.
     */
    public fun create_time_millis(create_time_millis: Long): Builder {
      this.create_time_millis = create_time_millis
      return this
    }

    /**
     * Which other services should be invoked after this interaction is complete.
     */
    public fun invoke_next(invoke_next: List<InvokeNext>): Builder {
      checkElementsNotNull(invoke_next)
      this.invoke_next = invoke_next
      return this
    }

    override fun build(): ClientDirective = ClientDirective(
      policy_reference = policy_reference,
      checkin_delay_millis = checkin_delay_millis,
      retry_attempts = retry_attempts,
      retry_period_millis = retry_period_millis,
      create_time_millis = create_time_millis,
      invoke_next = invoke_next,
      unknownFields = buildUnknownFields()
    )
  }

  public companion object {
    @JvmField
    public val ADAPTER: ProtoAdapter<ClientDirective> = object : ProtoAdapter<ClientDirective>(
      FieldEncoding.LENGTH_DELIMITED, 
      ClientDirective::class, 
      "type.googleapis.com/cryptauthv2.ClientDirective", 
      PROTO_3, 
      null, 
      "cryptauth/cryptauth_directive.proto"
    ) {
      override fun encodedSize(`value`: ClientDirective): Int {
        var size = value.unknownFields.size
        if (value.policy_reference != null) size += PolicyReference.ADAPTER.encodedSizeWithTag(1,
            value.policy_reference)
        if (value.checkin_delay_millis != 0L) size += ProtoAdapter.INT64.encodedSizeWithTag(2,
            value.checkin_delay_millis)
        if (value.retry_attempts != 0) size += ProtoAdapter.INT32.encodedSizeWithTag(3,
            value.retry_attempts)
        if (value.retry_period_millis != 0L) size += ProtoAdapter.INT64.encodedSizeWithTag(4,
            value.retry_period_millis)
        if (value.create_time_millis != 0L) size += ProtoAdapter.INT64.encodedSizeWithTag(5,
            value.create_time_millis)
        size += InvokeNext.ADAPTER.asRepeated().encodedSizeWithTag(6, value.invoke_next)
        return size
      }

      override fun encode(writer: ProtoWriter, `value`: ClientDirective) {
        if (value.policy_reference != null) PolicyReference.ADAPTER.encodeWithTag(writer, 1,
            value.policy_reference)
        if (value.checkin_delay_millis != 0L) ProtoAdapter.INT64.encodeWithTag(writer, 2,
            value.checkin_delay_millis)
        if (value.retry_attempts != 0) ProtoAdapter.INT32.encodeWithTag(writer, 3,
            value.retry_attempts)
        if (value.retry_period_millis != 0L) ProtoAdapter.INT64.encodeWithTag(writer, 4,
            value.retry_period_millis)
        if (value.create_time_millis != 0L) ProtoAdapter.INT64.encodeWithTag(writer, 5,
            value.create_time_millis)
        InvokeNext.ADAPTER.asRepeated().encodeWithTag(writer, 6, value.invoke_next)
        writer.writeBytes(value.unknownFields)
      }

      override fun encode(writer: ReverseProtoWriter, `value`: ClientDirective) {
        writer.writeBytes(value.unknownFields)
        InvokeNext.ADAPTER.asRepeated().encodeWithTag(writer, 6, value.invoke_next)
        if (value.create_time_millis != 0L) ProtoAdapter.INT64.encodeWithTag(writer, 5,
            value.create_time_millis)
        if (value.retry_period_millis != 0L) ProtoAdapter.INT64.encodeWithTag(writer, 4,
            value.retry_period_millis)
        if (value.retry_attempts != 0) ProtoAdapter.INT32.encodeWithTag(writer, 3,
            value.retry_attempts)
        if (value.checkin_delay_millis != 0L) ProtoAdapter.INT64.encodeWithTag(writer, 2,
            value.checkin_delay_millis)
        if (value.policy_reference != null) PolicyReference.ADAPTER.encodeWithTag(writer, 1,
            value.policy_reference)
      }

      override fun decode(reader: ProtoReader): ClientDirective {
        var policy_reference: PolicyReference? = null
        var checkin_delay_millis: Long = 0L
        var retry_attempts: Int = 0
        var retry_period_millis: Long = 0L
        var create_time_millis: Long = 0L
        val invoke_next = mutableListOf<InvokeNext>()
        val unknownFields = reader.forEachTag { tag ->
          when (tag) {
            1 -> policy_reference = PolicyReference.ADAPTER.decode(reader)
            2 -> checkin_delay_millis = ProtoAdapter.INT64.decode(reader)
            3 -> retry_attempts = ProtoAdapter.INT32.decode(reader)
            4 -> retry_period_millis = ProtoAdapter.INT64.decode(reader)
            5 -> create_time_millis = ProtoAdapter.INT64.decode(reader)
            6 -> invoke_next.add(InvokeNext.ADAPTER.decode(reader))
            else -> reader.readUnknownField(tag)
          }
        }
        return ClientDirective(
          policy_reference = policy_reference,
          checkin_delay_millis = checkin_delay_millis,
          retry_attempts = retry_attempts,
          retry_period_millis = retry_period_millis,
          create_time_millis = create_time_millis,
          invoke_next = invoke_next,
          unknownFields = unknownFields
        )
      }

      override fun redact(`value`: ClientDirective): ClientDirective = value.copy(
        policy_reference = value.policy_reference?.let(PolicyReference.ADAPTER::redact),
        invoke_next = value.invoke_next.redactElements(InvokeNext.ADAPTER),
        unknownFields = ByteString.EMPTY
      )
    }

    private const val serialVersionUID: Long = 0L

    @JvmSynthetic
    public inline fun build(body: Builder.() -> Unit): ClientDirective =
        Builder().apply(body).build()
  }
}
