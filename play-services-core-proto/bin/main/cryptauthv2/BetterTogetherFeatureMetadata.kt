// Code generated by Wire protocol buffer compiler, do not edit.
// Source: cryptauthv2.BetterTogetherFeatureMetadata in cryptauth/cryptauth_better_together_feature_metadata.proto
@file:Suppress("DEPRECATION")

package cryptauthv2

import com.squareup.wire.EnumAdapter
import com.squareup.wire.FieldEncoding
import com.squareup.wire.Message
import com.squareup.wire.ProtoAdapter
import com.squareup.wire.ProtoReader
import com.squareup.wire.ProtoWriter
import com.squareup.wire.ReverseProtoWriter
import com.squareup.wire.Syntax.PROTO_3
import com.squareup.wire.WireEnum
import com.squareup.wire.WireField
import com.squareup.wire.`internal`.JvmField
import com.squareup.wire.`internal`.JvmStatic
import com.squareup.wire.`internal`.JvmSynthetic
import com.squareup.wire.`internal`.checkElementsNotNull
import com.squareup.wire.`internal`.immutableCopyOf
import kotlin.Any
import kotlin.Boolean
import kotlin.Int
import kotlin.Long
import kotlin.String
import kotlin.Suppress
import kotlin.Unit
import kotlin.collections.List
import okio.ByteString

public class BetterTogetherFeatureMetadata(
  supported_features: List<FeatureName> = emptyList(),
  enabled_features: List<FeatureName> = emptyList(),
  unknownFields: ByteString = ByteString.EMPTY,
) : Message<BetterTogetherFeatureMetadata, BetterTogetherFeatureMetadata.Builder>(ADAPTER,
    unknownFields) {
  @field:WireField(
    tag = 1,
    adapter = "cryptauthv2.BetterTogetherFeatureMetadata${'$'}FeatureName#ADAPTER",
    label = WireField.Label.REPEATED,
    jsonName = "supportedFeatures",
    schemaIndex = 0,
  )
  @JvmField
  public val supported_features: List<FeatureName> = immutableCopyOf("supported_features",
      supported_features)

  @field:WireField(
    tag = 2,
    adapter = "cryptauthv2.BetterTogetherFeatureMetadata${'$'}FeatureName#ADAPTER",
    label = WireField.Label.REPEATED,
    jsonName = "enabledFeatures",
    schemaIndex = 1,
  )
  @JvmField
  public val enabled_features: List<FeatureName> = immutableCopyOf("enabled_features",
      enabled_features)

  override fun newBuilder(): Builder {
    val builder = Builder()
    builder.supported_features = supported_features
    builder.enabled_features = enabled_features
    builder.addUnknownFields(unknownFields)
    return builder
  }

  override fun equals(other: Any?): Boolean {
    if (other === this) return true
    if (other !is BetterTogetherFeatureMetadata) return false
    if (unknownFields != other.unknownFields) return false
    if (supported_features != other.supported_features) return false
    if (enabled_features != other.enabled_features) return false
    return true
  }

  override fun hashCode(): Int {
    var result = super.hashCode
    if (result == 0) {
      result = unknownFields.hashCode()
      result = result * 37 + supported_features.hashCode()
      result = result * 37 + enabled_features.hashCode()
      super.hashCode = result
    }
    return result
  }

  override fun toString(): String {
    val result = mutableListOf<String>()
    if (supported_features.isNotEmpty()) result += """supported_features=$supported_features"""
    if (enabled_features.isNotEmpty()) result += """enabled_features=$enabled_features"""
    return result.joinToString(prefix = "BetterTogetherFeatureMetadata{", separator = ", ", postfix
        = "}")
  }

  public fun copy(
    supported_features: List<FeatureName> = this.supported_features,
    enabled_features: List<FeatureName> = this.enabled_features,
    unknownFields: ByteString = this.unknownFields,
  ): BetterTogetherFeatureMetadata = BetterTogetherFeatureMetadata(supported_features,
      enabled_features, unknownFields)

  public class Builder : Message.Builder<BetterTogetherFeatureMetadata, Builder>() {
    @JvmField
    public var supported_features: List<FeatureName> = emptyList()

    @JvmField
    public var enabled_features: List<FeatureName> = emptyList()

    public fun supported_features(supported_features: List<FeatureName>): Builder {
      checkElementsNotNull(supported_features)
      this.supported_features = supported_features
      return this
    }

    public fun enabled_features(enabled_features: List<FeatureName>): Builder {
      checkElementsNotNull(enabled_features)
      this.enabled_features = enabled_features
      return this
    }

    override fun build(): BetterTogetherFeatureMetadata = BetterTogetherFeatureMetadata(
      supported_features = supported_features,
      enabled_features = enabled_features,
      unknownFields = buildUnknownFields()
    )
  }

  public companion object {
    @JvmField
    public val ADAPTER: ProtoAdapter<BetterTogetherFeatureMetadata> = object :
        ProtoAdapter<BetterTogetherFeatureMetadata>(
      FieldEncoding.LENGTH_DELIMITED, 
      BetterTogetherFeatureMetadata::class, 
      "type.googleapis.com/cryptauthv2.BetterTogetherFeatureMetadata", 
      PROTO_3, 
      null, 
      "cryptauth/cryptauth_better_together_feature_metadata.proto"
    ) {
      override fun encodedSize(`value`: BetterTogetherFeatureMetadata): Int {
        var size = value.unknownFields.size
        size += FeatureName.ADAPTER.asRepeated().encodedSizeWithTag(1, value.supported_features)
        size += FeatureName.ADAPTER.asRepeated().encodedSizeWithTag(2, value.enabled_features)
        return size
      }

      override fun encode(writer: ProtoWriter, `value`: BetterTogetherFeatureMetadata) {
        FeatureName.ADAPTER.asRepeated().encodeWithTag(writer, 1, value.supported_features)
        FeatureName.ADAPTER.asRepeated().encodeWithTag(writer, 2, value.enabled_features)
        writer.writeBytes(value.unknownFields)
      }

      override fun encode(writer: ReverseProtoWriter, `value`: BetterTogetherFeatureMetadata) {
        writer.writeBytes(value.unknownFields)
        FeatureName.ADAPTER.asRepeated().encodeWithTag(writer, 2, value.enabled_features)
        FeatureName.ADAPTER.asRepeated().encodeWithTag(writer, 1, value.supported_features)
      }

      override fun decode(reader: ProtoReader): BetterTogetherFeatureMetadata {
        val supported_features = mutableListOf<FeatureName>()
        val enabled_features = mutableListOf<FeatureName>()
        val unknownFields = reader.forEachTag { tag ->
          when (tag) {
            1 -> try {
              FeatureName.ADAPTER.tryDecode(reader, supported_features)
            } catch (e: ProtoAdapter.EnumConstantNotFoundException) {
              reader.addUnknownField(tag, FieldEncoding.VARINT, e.value.toLong())
            }
            2 -> try {
              FeatureName.ADAPTER.tryDecode(reader, enabled_features)
            } catch (e: ProtoAdapter.EnumConstantNotFoundException) {
              reader.addUnknownField(tag, FieldEncoding.VARINT, e.value.toLong())
            }
            else -> reader.readUnknownField(tag)
          }
        }
        return BetterTogetherFeatureMetadata(
          supported_features = supported_features,
          enabled_features = enabled_features,
          unknownFields = unknownFields
        )
      }

      override fun redact(`value`: BetterTogetherFeatureMetadata): BetterTogetherFeatureMetadata =
          value.copy(
        unknownFields = ByteString.EMPTY
      )
    }

    private const val serialVersionUID: Long = 0L

    @JvmSynthetic
    public inline fun build(body: Builder.() -> Unit): BetterTogetherFeatureMetadata =
        Builder().apply(body).build()
  }

  public enum class FeatureName(
    override val `value`: Int,
  ) : WireEnum {
    UNKNOWN_FEATURE(0),
    BETTER_TOGETHER_HOST(1),
    BETTER_TOGETHER_CLIENT(2),
    EASY_UNLOCK_HOST(3),
    EASY_UNLOCK_CLIENT(4),
    MAGIC_TETHER_HOST(5),
    MAGIC_TETHER_CLIENT(6),
    SMS_CONNECT_HOST(7),
    SMS_CONNECT_CLIENT(8),
    PHONE_HUB_HOST(9),
    PHONE_HUB_CLIENT(10),
    WIFI_SYNC_HOST(11),
    WIFI_SYNC_CLIENT(12),
    ECHE_HOST(13),
    ECHE_CLIENT(14),
    PHONE_HUB_CAMERA_ROLL_HOST(15),
    PHONE_HUB_CAMERA_ROLL_CLIENT(16),
    ;

    public companion object {
      @JvmField
      public val ADAPTER: ProtoAdapter<FeatureName> = object : EnumAdapter<FeatureName>(
        FeatureName::class, 
        PROTO_3, 
        FeatureName.UNKNOWN_FEATURE
      ) {
        override fun fromValue(`value`: Int): FeatureName? = FeatureName.fromValue(`value`)
      }

      @JvmStatic
      public fun fromValue(`value`: Int): FeatureName? = when (`value`) {
        0 -> UNKNOWN_FEATURE
        1 -> BETTER_TOGETHER_HOST
        2 -> BETTER_TOGETHER_CLIENT
        3 -> EASY_UNLOCK_HOST
        4 -> EASY_UNLOCK_CLIENT
        5 -> MAGIC_TETHER_HOST
        6 -> MAGIC_TETHER_CLIENT
        7 -> SMS_CONNECT_HOST
        8 -> SMS_CONNECT_CLIENT
        9 -> PHONE_HUB_HOST
        10 -> PHONE_HUB_CLIENT
        11 -> WIFI_SYNC_HOST
        12 -> WIFI_SYNC_CLIENT
        13 -> ECHE_HOST
        14 -> ECHE_CLIENT
        15 -> PHONE_HUB_CAMERA_ROLL_HOST
        16 -> PHONE_HUB_CAMERA_ROLL_CLIENT
        else -> null
      }
    }
  }
}
