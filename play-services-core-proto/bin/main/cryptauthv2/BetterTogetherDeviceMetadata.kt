// Code generated by Wire protocol buffer compiler, do not edit.
// Source: cryptauthv2.BetterTogetherDeviceMetadata in cryptauth/cryptauth_better_together_device_metadata.proto
@file:Suppress("DEPRECATION")

package cryptauthv2

import com.squareup.wire.FieldEncoding
import com.squareup.wire.Message
import com.squareup.wire.ProtoAdapter
import com.squareup.wire.ProtoReader
import com.squareup.wire.ProtoWriter
import com.squareup.wire.ReverseProtoWriter
import com.squareup.wire.Syntax.PROTO_3
import com.squareup.wire.WireField
import com.squareup.wire.`internal`.JvmField
import com.squareup.wire.`internal`.JvmSynthetic
import com.squareup.wire.`internal`.checkElementsNotNull
import com.squareup.wire.`internal`.immutableCopyOf
import com.squareup.wire.`internal`.redactElements
import com.squareup.wire.`internal`.sanitize
import kotlin.Any
import kotlin.Boolean
import kotlin.Int
import kotlin.Long
import kotlin.String
import kotlin.Suppress
import kotlin.Unit
import kotlin.collections.List
import okio.ByteString

/**
 * Device metadata relevant to the suite of multi-device (Better Together)
 * features. This data is sent to and received from CryptAuth--using end-to-end
 * encryption--as part of DeviceSync v2.
 * Next ID: 5
 */
public class BetterTogetherDeviceMetadata(
  /**
   * A cryptographic public key associated with the device.
   * The format of this key is a serialized SecureMessage.GenericPublicKey.
   */
  @field:WireField(
    tag = 1,
    adapter = "com.squareup.wire.ProtoAdapter#BYTES",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "publicKey",
    schemaIndex = 0,
  )
  @JvmField
  public val public_key: ByteString = ByteString.EMPTY,
  /**
   * A device model name that's guaranteed not to contain any PII.
   */
  @field:WireField(
    tag = 2,
    adapter = "com.squareup.wire.ProtoAdapter#STRING",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "noPiiDeviceName",
    schemaIndex = 1,
  )
  @JvmField
  public val no_pii_device_name: String = "",
  beacon_seeds: List<BeaconSeed> = emptyList(),
  /**
   * Bluetooth public address, formatted as a hex string with colons and capital
   * letters. Example: "01:23:45:67:89:AB"
   */
  @field:WireField(
    tag = 4,
    adapter = "com.squareup.wire.ProtoAdapter#STRING",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "bluetoothPublicAddress",
    schemaIndex = 3,
  )
  @JvmField
  public val bluetooth_public_address: String = "",
  /**
   * Attestation data associated with this device.
   */
  @field:WireField(
    tag = 5,
    adapter = "cryptauthv2.AttestationData#ADAPTER",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "attestationData",
    schemaIndex = 4,
  )
  @JvmField
  public val attestation_data: AttestationData? = null,
  unknownFields: ByteString = ByteString.EMPTY,
) : Message<BetterTogetherDeviceMetadata, BetterTogetherDeviceMetadata.Builder>(ADAPTER,
    unknownFields) {
  /**
   * A list of seeds for EID BLE advertisements targeting this device.
   */
  @field:WireField(
    tag = 3,
    adapter = "cryptauthv2.BeaconSeed#ADAPTER",
    label = WireField.Label.REPEATED,
    jsonName = "beaconSeeds",
    schemaIndex = 2,
  )
  @JvmField
  public val beacon_seeds: List<BeaconSeed> = immutableCopyOf("beacon_seeds", beacon_seeds)

  override fun newBuilder(): Builder {
    val builder = Builder()
    builder.public_key = public_key
    builder.no_pii_device_name = no_pii_device_name
    builder.beacon_seeds = beacon_seeds
    builder.bluetooth_public_address = bluetooth_public_address
    builder.attestation_data = attestation_data
    builder.addUnknownFields(unknownFields)
    return builder
  }

  override fun equals(other: Any?): Boolean {
    if (other === this) return true
    if (other !is BetterTogetherDeviceMetadata) return false
    if (unknownFields != other.unknownFields) return false
    if (public_key != other.public_key) return false
    if (no_pii_device_name != other.no_pii_device_name) return false
    if (beacon_seeds != other.beacon_seeds) return false
    if (bluetooth_public_address != other.bluetooth_public_address) return false
    if (attestation_data != other.attestation_data) return false
    return true
  }

  override fun hashCode(): Int {
    var result = super.hashCode
    if (result == 0) {
      result = unknownFields.hashCode()
      result = result * 37 + public_key.hashCode()
      result = result * 37 + no_pii_device_name.hashCode()
      result = result * 37 + beacon_seeds.hashCode()
      result = result * 37 + bluetooth_public_address.hashCode()
      result = result * 37 + (attestation_data?.hashCode() ?: 0)
      super.hashCode = result
    }
    return result
  }

  override fun toString(): String {
    val result = mutableListOf<String>()
    result += """public_key=$public_key"""
    result += """no_pii_device_name=${sanitize(no_pii_device_name)}"""
    if (beacon_seeds.isNotEmpty()) result += """beacon_seeds=$beacon_seeds"""
    result += """bluetooth_public_address=${sanitize(bluetooth_public_address)}"""
    if (attestation_data != null) result += """attestation_data=$attestation_data"""
    return result.joinToString(prefix = "BetterTogetherDeviceMetadata{", separator = ", ", postfix =
        "}")
  }

  public fun copy(
    public_key: ByteString = this.public_key,
    no_pii_device_name: String = this.no_pii_device_name,
    beacon_seeds: List<BeaconSeed> = this.beacon_seeds,
    bluetooth_public_address: String = this.bluetooth_public_address,
    attestation_data: AttestationData? = this.attestation_data,
    unknownFields: ByteString = this.unknownFields,
  ): BetterTogetherDeviceMetadata = BetterTogetherDeviceMetadata(public_key, no_pii_device_name,
      beacon_seeds, bluetooth_public_address, attestation_data, unknownFields)

  public class Builder : Message.Builder<BetterTogetherDeviceMetadata, Builder>() {
    @JvmField
    public var public_key: ByteString = ByteString.EMPTY

    @JvmField
    public var no_pii_device_name: String = ""

    @JvmField
    public var beacon_seeds: List<BeaconSeed> = emptyList()

    @JvmField
    public var bluetooth_public_address: String = ""

    @JvmField
    public var attestation_data: AttestationData? = null

    /**
     * A cryptographic public key associated with the device.
     * The format of this key is a serialized SecureMessage.GenericPublicKey.
     */
    public fun public_key(public_key: ByteString): Builder {
      this.public_key = public_key
      return this
    }

    /**
     * A device model name that's guaranteed not to contain any PII.
     */
    public fun no_pii_device_name(no_pii_device_name: String): Builder {
      this.no_pii_device_name = no_pii_device_name
      return this
    }

    /**
     * A list of seeds for EID BLE advertisements targeting this device.
     */
    public fun beacon_seeds(beacon_seeds: List<BeaconSeed>): Builder {
      checkElementsNotNull(beacon_seeds)
      this.beacon_seeds = beacon_seeds
      return this
    }

    /**
     * Bluetooth public address, formatted as a hex string with colons and capital
     * letters. Example: "01:23:45:67:89:AB"
     */
    public fun bluetooth_public_address(bluetooth_public_address: String): Builder {
      this.bluetooth_public_address = bluetooth_public_address
      return this
    }

    /**
     * Attestation data associated with this device.
     */
    public fun attestation_data(attestation_data: AttestationData?): Builder {
      this.attestation_data = attestation_data
      return this
    }

    override fun build(): BetterTogetherDeviceMetadata = BetterTogetherDeviceMetadata(
      public_key = public_key,
      no_pii_device_name = no_pii_device_name,
      beacon_seeds = beacon_seeds,
      bluetooth_public_address = bluetooth_public_address,
      attestation_data = attestation_data,
      unknownFields = buildUnknownFields()
    )
  }

  public companion object {
    @JvmField
    public val ADAPTER: ProtoAdapter<BetterTogetherDeviceMetadata> = object :
        ProtoAdapter<BetterTogetherDeviceMetadata>(
      FieldEncoding.LENGTH_DELIMITED, 
      BetterTogetherDeviceMetadata::class, 
      "type.googleapis.com/cryptauthv2.BetterTogetherDeviceMetadata", 
      PROTO_3, 
      null, 
      "cryptauth/cryptauth_better_together_device_metadata.proto"
    ) {
      override fun encodedSize(`value`: BetterTogetherDeviceMetadata): Int {
        var size = value.unknownFields.size
        if (value.public_key != ByteString.EMPTY) size += ProtoAdapter.BYTES.encodedSizeWithTag(1,
            value.public_key)
        if (value.no_pii_device_name != "") size += ProtoAdapter.STRING.encodedSizeWithTag(2,
            value.no_pii_device_name)
        size += BeaconSeed.ADAPTER.asRepeated().encodedSizeWithTag(3, value.beacon_seeds)
        if (value.bluetooth_public_address != "") size += ProtoAdapter.STRING.encodedSizeWithTag(4,
            value.bluetooth_public_address)
        if (value.attestation_data != null) size += AttestationData.ADAPTER.encodedSizeWithTag(5,
            value.attestation_data)
        return size
      }

      override fun encode(writer: ProtoWriter, `value`: BetterTogetherDeviceMetadata) {
        if (value.public_key != ByteString.EMPTY) ProtoAdapter.BYTES.encodeWithTag(writer, 1,
            value.public_key)
        if (value.no_pii_device_name != "") ProtoAdapter.STRING.encodeWithTag(writer, 2,
            value.no_pii_device_name)
        BeaconSeed.ADAPTER.asRepeated().encodeWithTag(writer, 3, value.beacon_seeds)
        if (value.bluetooth_public_address != "") ProtoAdapter.STRING.encodeWithTag(writer, 4,
            value.bluetooth_public_address)
        if (value.attestation_data != null) AttestationData.ADAPTER.encodeWithTag(writer, 5,
            value.attestation_data)
        writer.writeBytes(value.unknownFields)
      }

      override fun encode(writer: ReverseProtoWriter, `value`: BetterTogetherDeviceMetadata) {
        writer.writeBytes(value.unknownFields)
        if (value.attestation_data != null) AttestationData.ADAPTER.encodeWithTag(writer, 5,
            value.attestation_data)
        if (value.bluetooth_public_address != "") ProtoAdapter.STRING.encodeWithTag(writer, 4,
            value.bluetooth_public_address)
        BeaconSeed.ADAPTER.asRepeated().encodeWithTag(writer, 3, value.beacon_seeds)
        if (value.no_pii_device_name != "") ProtoAdapter.STRING.encodeWithTag(writer, 2,
            value.no_pii_device_name)
        if (value.public_key != ByteString.EMPTY) ProtoAdapter.BYTES.encodeWithTag(writer, 1,
            value.public_key)
      }

      override fun decode(reader: ProtoReader): BetterTogetherDeviceMetadata {
        var public_key: ByteString = ByteString.EMPTY
        var no_pii_device_name: String = ""
        val beacon_seeds = mutableListOf<BeaconSeed>()
        var bluetooth_public_address: String = ""
        var attestation_data: AttestationData? = null
        val unknownFields = reader.forEachTag { tag ->
          when (tag) {
            1 -> public_key = ProtoAdapter.BYTES.decode(reader)
            2 -> no_pii_device_name = ProtoAdapter.STRING.decode(reader)
            3 -> beacon_seeds.add(BeaconSeed.ADAPTER.decode(reader))
            4 -> bluetooth_public_address = ProtoAdapter.STRING.decode(reader)
            5 -> attestation_data = AttestationData.ADAPTER.decode(reader)
            else -> reader.readUnknownField(tag)
          }
        }
        return BetterTogetherDeviceMetadata(
          public_key = public_key,
          no_pii_device_name = no_pii_device_name,
          beacon_seeds = beacon_seeds,
          bluetooth_public_address = bluetooth_public_address,
          attestation_data = attestation_data,
          unknownFields = unknownFields
        )
      }

      override fun redact(`value`: BetterTogetherDeviceMetadata): BetterTogetherDeviceMetadata =
          value.copy(
        beacon_seeds = value.beacon_seeds.redactElements(BeaconSeed.ADAPTER),
        attestation_data = value.attestation_data?.let(AttestationData.ADAPTER::redact),
        unknownFields = ByteString.EMPTY
      )
    }

    private const val serialVersionUID: Long = 0L

    @JvmSynthetic
    public inline fun build(body: Builder.() -> Unit): BetterTogetherDeviceMetadata =
        Builder().apply(body).build()
  }
}
