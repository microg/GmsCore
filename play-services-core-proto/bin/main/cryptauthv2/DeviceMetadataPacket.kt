// Code generated by Wire protocol buffer compiler, do not edit.
// Source: cryptauthv2.DeviceMetadataPacket in cryptauth/cryptauth_devicesync.proto
@file:Suppress("DEPRECATION")

package cryptauthv2

import com.squareup.wire.FieldEncoding
import com.squareup.wire.Message
import com.squareup.wire.ProtoAdapter
import com.squareup.wire.ProtoReader
import com.squareup.wire.ProtoWriter
import com.squareup.wire.ReverseProtoWriter
import com.squareup.wire.Syntax.PROTO_3
import com.squareup.wire.WireField
import com.squareup.wire.`internal`.JvmField
import com.squareup.wire.`internal`.JvmSynthetic
import com.squareup.wire.`internal`.sanitize
import kotlin.Any
import kotlin.Boolean
import kotlin.Int
import kotlin.Long
import kotlin.String
import kotlin.Suppress
import kotlin.Unit
import okio.ByteString

/**
 * One device's metadata, containing an identifier for the particular device,
 * along with its encrypted metadata.
 */
public class DeviceMetadataPacket(
  /**
   * Device identifier.
   */
  @field:WireField(
    tag = 1,
    adapter = "com.squareup.wire.ProtoAdapter#STRING",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "deviceId",
    schemaIndex = 0,
  )
  @JvmField
  public val device_id: String = "",
  /**
   * This device's metadata, encrypted with the group's private key.
   */
  @field:WireField(
    tag = 2,
    adapter = "com.squareup.wire.ProtoAdapter#BYTES",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "encryptedMetadata",
    schemaIndex = 1,
  )
  @JvmField
  public val encrypted_metadata: ByteString = ByteString.EMPTY,
  /**
   * Indicates that the associated device needs the group's private key.
   */
  @field:WireField(
    tag = 3,
    adapter = "com.squareup.wire.ProtoAdapter#BOOL",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "needGroupPrivateKey",
    schemaIndex = 2,
  )
  @JvmField
  public val need_group_private_key: Boolean = false,
  /**
   * The associated device's public key, to be used with
   * EncryptedGroupPrivateKey below.
   */
  @field:WireField(
    tag = 4,
    adapter = "com.squareup.wire.ProtoAdapter#BYTES",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "devicePublicKey",
    schemaIndex = 3,
  )
  @JvmField
  public val device_public_key: ByteString = ByteString.EMPTY,
  /**
   * A name known to the server or which was assigned by the user to the device.
   */
  @field:WireField(
    tag = 5,
    adapter = "com.squareup.wire.ProtoAdapter#STRING",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "deviceName",
    schemaIndex = 4,
  )
  @JvmField
  public val device_name: String = "",
  unknownFields: ByteString = ByteString.EMPTY,
) : Message<DeviceMetadataPacket, DeviceMetadataPacket.Builder>(ADAPTER, unknownFields) {
  override fun newBuilder(): Builder {
    val builder = Builder()
    builder.device_id = device_id
    builder.encrypted_metadata = encrypted_metadata
    builder.need_group_private_key = need_group_private_key
    builder.device_public_key = device_public_key
    builder.device_name = device_name
    builder.addUnknownFields(unknownFields)
    return builder
  }

  override fun equals(other: Any?): Boolean {
    if (other === this) return true
    if (other !is DeviceMetadataPacket) return false
    if (unknownFields != other.unknownFields) return false
    if (device_id != other.device_id) return false
    if (encrypted_metadata != other.encrypted_metadata) return false
    if (need_group_private_key != other.need_group_private_key) return false
    if (device_public_key != other.device_public_key) return false
    if (device_name != other.device_name) return false
    return true
  }

  override fun hashCode(): Int {
    var result = super.hashCode
    if (result == 0) {
      result = unknownFields.hashCode()
      result = result * 37 + device_id.hashCode()
      result = result * 37 + encrypted_metadata.hashCode()
      result = result * 37 + need_group_private_key.hashCode()
      result = result * 37 + device_public_key.hashCode()
      result = result * 37 + device_name.hashCode()
      super.hashCode = result
    }
    return result
  }

  override fun toString(): String {
    val result = mutableListOf<String>()
    result += """device_id=${sanitize(device_id)}"""
    result += """encrypted_metadata=$encrypted_metadata"""
    result += """need_group_private_key=$need_group_private_key"""
    result += """device_public_key=$device_public_key"""
    result += """device_name=${sanitize(device_name)}"""
    return result.joinToString(prefix = "DeviceMetadataPacket{", separator = ", ", postfix = "}")
  }

  public fun copy(
    device_id: String = this.device_id,
    encrypted_metadata: ByteString = this.encrypted_metadata,
    need_group_private_key: Boolean = this.need_group_private_key,
    device_public_key: ByteString = this.device_public_key,
    device_name: String = this.device_name,
    unknownFields: ByteString = this.unknownFields,
  ): DeviceMetadataPacket = DeviceMetadataPacket(device_id, encrypted_metadata,
      need_group_private_key, device_public_key, device_name, unknownFields)

  public class Builder : Message.Builder<DeviceMetadataPacket, Builder>() {
    @JvmField
    public var device_id: String = ""

    @JvmField
    public var encrypted_metadata: ByteString = ByteString.EMPTY

    @JvmField
    public var need_group_private_key: Boolean = false

    @JvmField
    public var device_public_key: ByteString = ByteString.EMPTY

    @JvmField
    public var device_name: String = ""

    /**
     * Device identifier.
     */
    public fun device_id(device_id: String): Builder {
      this.device_id = device_id
      return this
    }

    /**
     * This device's metadata, encrypted with the group's private key.
     */
    public fun encrypted_metadata(encrypted_metadata: ByteString): Builder {
      this.encrypted_metadata = encrypted_metadata
      return this
    }

    /**
     * Indicates that the associated device needs the group's private key.
     */
    public fun need_group_private_key(need_group_private_key: Boolean): Builder {
      this.need_group_private_key = need_group_private_key
      return this
    }

    /**
     * The associated device's public key, to be used with
     * EncryptedGroupPrivateKey below.
     */
    public fun device_public_key(device_public_key: ByteString): Builder {
      this.device_public_key = device_public_key
      return this
    }

    /**
     * A name known to the server or which was assigned by the user to the device.
     */
    public fun device_name(device_name: String): Builder {
      this.device_name = device_name
      return this
    }

    override fun build(): DeviceMetadataPacket = DeviceMetadataPacket(
      device_id = device_id,
      encrypted_metadata = encrypted_metadata,
      need_group_private_key = need_group_private_key,
      device_public_key = device_public_key,
      device_name = device_name,
      unknownFields = buildUnknownFields()
    )
  }

  public companion object {
    @JvmField
    public val ADAPTER: ProtoAdapter<DeviceMetadataPacket> = object :
        ProtoAdapter<DeviceMetadataPacket>(
      FieldEncoding.LENGTH_DELIMITED, 
      DeviceMetadataPacket::class, 
      "type.googleapis.com/cryptauthv2.DeviceMetadataPacket", 
      PROTO_3, 
      null, 
      "cryptauth/cryptauth_devicesync.proto"
    ) {
      override fun encodedSize(`value`: DeviceMetadataPacket): Int {
        var size = value.unknownFields.size
        if (value.device_id != "") size += ProtoAdapter.STRING.encodedSizeWithTag(1,
            value.device_id)
        if (value.encrypted_metadata != ByteString.EMPTY) size +=
            ProtoAdapter.BYTES.encodedSizeWithTag(2, value.encrypted_metadata)
        if (value.need_group_private_key != false) size += ProtoAdapter.BOOL.encodedSizeWithTag(3,
            value.need_group_private_key)
        if (value.device_public_key != ByteString.EMPTY) size +=
            ProtoAdapter.BYTES.encodedSizeWithTag(4, value.device_public_key)
        if (value.device_name != "") size += ProtoAdapter.STRING.encodedSizeWithTag(5,
            value.device_name)
        return size
      }

      override fun encode(writer: ProtoWriter, `value`: DeviceMetadataPacket) {
        if (value.device_id != "") ProtoAdapter.STRING.encodeWithTag(writer, 1, value.device_id)
        if (value.encrypted_metadata != ByteString.EMPTY) ProtoAdapter.BYTES.encodeWithTag(writer,
            2, value.encrypted_metadata)
        if (value.need_group_private_key != false) ProtoAdapter.BOOL.encodeWithTag(writer, 3,
            value.need_group_private_key)
        if (value.device_public_key != ByteString.EMPTY) ProtoAdapter.BYTES.encodeWithTag(writer, 4,
            value.device_public_key)
        if (value.device_name != "") ProtoAdapter.STRING.encodeWithTag(writer, 5, value.device_name)
        writer.writeBytes(value.unknownFields)
      }

      override fun encode(writer: ReverseProtoWriter, `value`: DeviceMetadataPacket) {
        writer.writeBytes(value.unknownFields)
        if (value.device_name != "") ProtoAdapter.STRING.encodeWithTag(writer, 5, value.device_name)
        if (value.device_public_key != ByteString.EMPTY) ProtoAdapter.BYTES.encodeWithTag(writer, 4,
            value.device_public_key)
        if (value.need_group_private_key != false) ProtoAdapter.BOOL.encodeWithTag(writer, 3,
            value.need_group_private_key)
        if (value.encrypted_metadata != ByteString.EMPTY) ProtoAdapter.BYTES.encodeWithTag(writer,
            2, value.encrypted_metadata)
        if (value.device_id != "") ProtoAdapter.STRING.encodeWithTag(writer, 1, value.device_id)
      }

      override fun decode(reader: ProtoReader): DeviceMetadataPacket {
        var device_id: String = ""
        var encrypted_metadata: ByteString = ByteString.EMPTY
        var need_group_private_key: Boolean = false
        var device_public_key: ByteString = ByteString.EMPTY
        var device_name: String = ""
        val unknownFields = reader.forEachTag { tag ->
          when (tag) {
            1 -> device_id = ProtoAdapter.STRING.decode(reader)
            2 -> encrypted_metadata = ProtoAdapter.BYTES.decode(reader)
            3 -> need_group_private_key = ProtoAdapter.BOOL.decode(reader)
            4 -> device_public_key = ProtoAdapter.BYTES.decode(reader)
            5 -> device_name = ProtoAdapter.STRING.decode(reader)
            else -> reader.readUnknownField(tag)
          }
        }
        return DeviceMetadataPacket(
          device_id = device_id,
          encrypted_metadata = encrypted_metadata,
          need_group_private_key = need_group_private_key,
          device_public_key = device_public_key,
          device_name = device_name,
          unknownFields = unknownFields
        )
      }

      override fun redact(`value`: DeviceMetadataPacket): DeviceMetadataPacket = value.copy(
        unknownFields = ByteString.EMPTY
      )
    }

    private const val serialVersionUID: Long = 0L

    @JvmSynthetic
    public inline fun build(body: Builder.() -> Unit): DeviceMetadataPacket =
        Builder().apply(body).build()
  }
}
