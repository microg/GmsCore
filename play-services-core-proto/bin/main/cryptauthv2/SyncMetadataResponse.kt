// Code generated by Wire protocol buffer compiler, do not edit.
// Source: cryptauthv2.SyncMetadataResponse in cryptauth/cryptauth_devicesync.proto
@file:Suppress("DEPRECATION")

package cryptauthv2

import com.squareup.wire.FieldEncoding
import com.squareup.wire.Message
import com.squareup.wire.ProtoAdapter
import com.squareup.wire.ProtoReader
import com.squareup.wire.ProtoWriter
import com.squareup.wire.ReverseProtoWriter
import com.squareup.wire.Syntax.PROTO_3
import com.squareup.wire.WireField
import com.squareup.wire.`internal`.JvmField
import com.squareup.wire.`internal`.JvmSynthetic
import com.squareup.wire.`internal`.checkElementsNotNull
import com.squareup.wire.`internal`.immutableCopyOf
import com.squareup.wire.`internal`.redactElements
import kotlin.Any
import kotlin.Boolean
import kotlin.Int
import kotlin.Long
import kotlin.String
import kotlin.Suppress
import kotlin.Unit
import kotlin.collections.List
import okio.ByteString

/**
 * Response from server with any new devices' encrypted metadata and public key.
 */
public class SyncMetadataResponse(
  encrypted_metadata: List<DeviceMetadataPacket> = emptyList(),
  /**
   * Public key associated with the group, used to encrypt all metadata.
   * May be different than key received in the request.
   * If not set, the server is indicating a new group key pair must be created
   * by this device.
   */
  @field:WireField(
    tag = 2,
    adapter = "com.squareup.wire.ProtoAdapter#BYTES",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "groupPublicKey",
    schemaIndex = 1,
  )
  @JvmField
  public val group_public_key: ByteString = ByteString.EMPTY,
  /**
   * An encrypted group private key that contains device public key used for
   * encryption. Encrypted with the public key of the device.
   */
  @field:WireField(
    tag = 3,
    adapter = "cryptauthv2.EncryptedGroupPrivateKey#ADAPTER",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "encryptedGroupPrivateKey",
    schemaIndex = 2,
  )
  @JvmField
  public val encrypted_group_private_key: EncryptedGroupPrivateKey? = null,
  /**
   * Updated freshness token from the server.
   * Use this value in subsequent requests, to obtain only data updated since
   * the last request.
   */
  @field:WireField(
    tag = 4,
    adapter = "com.squareup.wire.ProtoAdapter#BYTES",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "freshnessToken",
    schemaIndex = 3,
  )
  @JvmField
  public val freshness_token: ByteString = ByteString.EMPTY,
  /**
   * The updated client-specific directives.
   */
  @field:WireField(
    tag = 5,
    adapter = "cryptauthv2.ClientDirective#ADAPTER",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "clientDirective",
    schemaIndex = 4,
  )
  @JvmField
  public val client_directive: ClientDirective? = null,
  unknownFields: ByteString = ByteString.EMPTY,
) : Message<SyncMetadataResponse, SyncMetadataResponse.Builder>(ADAPTER, unknownFields) {
  /**
   * Collection of encrypted metadata from devices that have been updated since
   * last provided refresh_token. All metadata if none was provided.
   */
  @field:WireField(
    tag = 1,
    adapter = "cryptauthv2.DeviceMetadataPacket#ADAPTER",
    label = WireField.Label.REPEATED,
    jsonName = "encryptedMetadata",
    schemaIndex = 0,
  )
  @JvmField
  public val encrypted_metadata: List<DeviceMetadataPacket> = immutableCopyOf("encrypted_metadata",
      encrypted_metadata)

  override fun newBuilder(): Builder {
    val builder = Builder()
    builder.encrypted_metadata = encrypted_metadata
    builder.group_public_key = group_public_key
    builder.encrypted_group_private_key = encrypted_group_private_key
    builder.freshness_token = freshness_token
    builder.client_directive = client_directive
    builder.addUnknownFields(unknownFields)
    return builder
  }

  override fun equals(other: Any?): Boolean {
    if (other === this) return true
    if (other !is SyncMetadataResponse) return false
    if (unknownFields != other.unknownFields) return false
    if (encrypted_metadata != other.encrypted_metadata) return false
    if (group_public_key != other.group_public_key) return false
    if (encrypted_group_private_key != other.encrypted_group_private_key) return false
    if (freshness_token != other.freshness_token) return false
    if (client_directive != other.client_directive) return false
    return true
  }

  override fun hashCode(): Int {
    var result = super.hashCode
    if (result == 0) {
      result = unknownFields.hashCode()
      result = result * 37 + encrypted_metadata.hashCode()
      result = result * 37 + group_public_key.hashCode()
      result = result * 37 + (encrypted_group_private_key?.hashCode() ?: 0)
      result = result * 37 + freshness_token.hashCode()
      result = result * 37 + (client_directive?.hashCode() ?: 0)
      super.hashCode = result
    }
    return result
  }

  override fun toString(): String {
    val result = mutableListOf<String>()
    if (encrypted_metadata.isNotEmpty()) result += """encrypted_metadata=$encrypted_metadata"""
    result += """group_public_key=$group_public_key"""
    if (encrypted_group_private_key != null) result +=
        """encrypted_group_private_key=$encrypted_group_private_key"""
    result += """freshness_token=$freshness_token"""
    if (client_directive != null) result += """client_directive=$client_directive"""
    return result.joinToString(prefix = "SyncMetadataResponse{", separator = ", ", postfix = "}")
  }

  public fun copy(
    encrypted_metadata: List<DeviceMetadataPacket> = this.encrypted_metadata,
    group_public_key: ByteString = this.group_public_key,
    encrypted_group_private_key: EncryptedGroupPrivateKey? = this.encrypted_group_private_key,
    freshness_token: ByteString = this.freshness_token,
    client_directive: ClientDirective? = this.client_directive,
    unknownFields: ByteString = this.unknownFields,
  ): SyncMetadataResponse = SyncMetadataResponse(encrypted_metadata, group_public_key,
      encrypted_group_private_key, freshness_token, client_directive, unknownFields)

  public class Builder : Message.Builder<SyncMetadataResponse, Builder>() {
    @JvmField
    public var encrypted_metadata: List<DeviceMetadataPacket> = emptyList()

    @JvmField
    public var group_public_key: ByteString = ByteString.EMPTY

    @JvmField
    public var encrypted_group_private_key: EncryptedGroupPrivateKey? = null

    @JvmField
    public var freshness_token: ByteString = ByteString.EMPTY

    @JvmField
    public var client_directive: ClientDirective? = null

    /**
     * Collection of encrypted metadata from devices that have been updated since
     * last provided refresh_token. All metadata if none was provided.
     */
    public fun encrypted_metadata(encrypted_metadata: List<DeviceMetadataPacket>): Builder {
      checkElementsNotNull(encrypted_metadata)
      this.encrypted_metadata = encrypted_metadata
      return this
    }

    /**
     * Public key associated with the group, used to encrypt all metadata.
     * May be different than key received in the request.
     * If not set, the server is indicating a new group key pair must be created
     * by this device.
     */
    public fun group_public_key(group_public_key: ByteString): Builder {
      this.group_public_key = group_public_key
      return this
    }

    /**
     * An encrypted group private key that contains device public key used for
     * encryption. Encrypted with the public key of the device.
     */
    public fun encrypted_group_private_key(encrypted_group_private_key: EncryptedGroupPrivateKey?):
        Builder {
      this.encrypted_group_private_key = encrypted_group_private_key
      return this
    }

    /**
     * Updated freshness token from the server.
     * Use this value in subsequent requests, to obtain only data updated since
     * the last request.
     */
    public fun freshness_token(freshness_token: ByteString): Builder {
      this.freshness_token = freshness_token
      return this
    }

    /**
     * The updated client-specific directives.
     */
    public fun client_directive(client_directive: ClientDirective?): Builder {
      this.client_directive = client_directive
      return this
    }

    override fun build(): SyncMetadataResponse = SyncMetadataResponse(
      encrypted_metadata = encrypted_metadata,
      group_public_key = group_public_key,
      encrypted_group_private_key = encrypted_group_private_key,
      freshness_token = freshness_token,
      client_directive = client_directive,
      unknownFields = buildUnknownFields()
    )
  }

  public companion object {
    @JvmField
    public val ADAPTER: ProtoAdapter<SyncMetadataResponse> = object :
        ProtoAdapter<SyncMetadataResponse>(
      FieldEncoding.LENGTH_DELIMITED, 
      SyncMetadataResponse::class, 
      "type.googleapis.com/cryptauthv2.SyncMetadataResponse", 
      PROTO_3, 
      null, 
      "cryptauth/cryptauth_devicesync.proto"
    ) {
      override fun encodedSize(`value`: SyncMetadataResponse): Int {
        var size = value.unknownFields.size
        size += DeviceMetadataPacket.ADAPTER.asRepeated().encodedSizeWithTag(1,
            value.encrypted_metadata)
        if (value.group_public_key != ByteString.EMPTY) size +=
            ProtoAdapter.BYTES.encodedSizeWithTag(2, value.group_public_key)
        if (value.encrypted_group_private_key != null) size +=
            EncryptedGroupPrivateKey.ADAPTER.encodedSizeWithTag(3,
            value.encrypted_group_private_key)
        if (value.freshness_token != ByteString.EMPTY) size +=
            ProtoAdapter.BYTES.encodedSizeWithTag(4, value.freshness_token)
        if (value.client_directive != null) size += ClientDirective.ADAPTER.encodedSizeWithTag(5,
            value.client_directive)
        return size
      }

      override fun encode(writer: ProtoWriter, `value`: SyncMetadataResponse) {
        DeviceMetadataPacket.ADAPTER.asRepeated().encodeWithTag(writer, 1, value.encrypted_metadata)
        if (value.group_public_key != ByteString.EMPTY) ProtoAdapter.BYTES.encodeWithTag(writer, 2,
            value.group_public_key)
        if (value.encrypted_group_private_key != null)
            EncryptedGroupPrivateKey.ADAPTER.encodeWithTag(writer, 3,
            value.encrypted_group_private_key)
        if (value.freshness_token != ByteString.EMPTY) ProtoAdapter.BYTES.encodeWithTag(writer, 4,
            value.freshness_token)
        if (value.client_directive != null) ClientDirective.ADAPTER.encodeWithTag(writer, 5,
            value.client_directive)
        writer.writeBytes(value.unknownFields)
      }

      override fun encode(writer: ReverseProtoWriter, `value`: SyncMetadataResponse) {
        writer.writeBytes(value.unknownFields)
        if (value.client_directive != null) ClientDirective.ADAPTER.encodeWithTag(writer, 5,
            value.client_directive)
        if (value.freshness_token != ByteString.EMPTY) ProtoAdapter.BYTES.encodeWithTag(writer, 4,
            value.freshness_token)
        if (value.encrypted_group_private_key != null)
            EncryptedGroupPrivateKey.ADAPTER.encodeWithTag(writer, 3,
            value.encrypted_group_private_key)
        if (value.group_public_key != ByteString.EMPTY) ProtoAdapter.BYTES.encodeWithTag(writer, 2,
            value.group_public_key)
        DeviceMetadataPacket.ADAPTER.asRepeated().encodeWithTag(writer, 1, value.encrypted_metadata)
      }

      override fun decode(reader: ProtoReader): SyncMetadataResponse {
        val encrypted_metadata = mutableListOf<DeviceMetadataPacket>()
        var group_public_key: ByteString = ByteString.EMPTY
        var encrypted_group_private_key: EncryptedGroupPrivateKey? = null
        var freshness_token: ByteString = ByteString.EMPTY
        var client_directive: ClientDirective? = null
        val unknownFields = reader.forEachTag { tag ->
          when (tag) {
            1 -> encrypted_metadata.add(DeviceMetadataPacket.ADAPTER.decode(reader))
            2 -> group_public_key = ProtoAdapter.BYTES.decode(reader)
            3 -> encrypted_group_private_key = EncryptedGroupPrivateKey.ADAPTER.decode(reader)
            4 -> freshness_token = ProtoAdapter.BYTES.decode(reader)
            5 -> client_directive = ClientDirective.ADAPTER.decode(reader)
            else -> reader.readUnknownField(tag)
          }
        }
        return SyncMetadataResponse(
          encrypted_metadata = encrypted_metadata,
          group_public_key = group_public_key,
          encrypted_group_private_key = encrypted_group_private_key,
          freshness_token = freshness_token,
          client_directive = client_directive,
          unknownFields = unknownFields
        )
      }

      override fun redact(`value`: SyncMetadataResponse): SyncMetadataResponse = value.copy(
        encrypted_metadata = value.encrypted_metadata.redactElements(DeviceMetadataPacket.ADAPTER),
        encrypted_group_private_key =
            value.encrypted_group_private_key?.let(EncryptedGroupPrivateKey.ADAPTER::redact),
        client_directive = value.client_directive?.let(ClientDirective.ADAPTER::redact),
        unknownFields = ByteString.EMPTY
      )
    }

    private const val serialVersionUID: Long = 0L

    @JvmSynthetic
    public inline fun build(body: Builder.() -> Unit): SyncMetadataResponse =
        Builder().apply(body).build()
  }
}
