// Code generated by Wire protocol buffer compiler, do not edit.
// Source: cryptauthv2.SyncMetadataRequest in cryptauth/cryptauth_devicesync.proto
@file:Suppress("DEPRECATION")

package cryptauthv2

import com.squareup.wire.FieldEncoding
import com.squareup.wire.Message
import com.squareup.wire.ProtoAdapter
import com.squareup.wire.ProtoReader
import com.squareup.wire.ProtoWriter
import com.squareup.wire.ReverseProtoWriter
import com.squareup.wire.Syntax.PROTO_3
import com.squareup.wire.WireField
import com.squareup.wire.`internal`.JvmField
import com.squareup.wire.`internal`.JvmSynthetic
import kotlin.Any
import kotlin.Boolean
import kotlin.Int
import kotlin.Long
import kotlin.String
import kotlin.Suppress
import kotlin.Unit
import okio.ByteString

/**
 * Requests from a client to sync its metadata and receive encrypted copy of
 * other metadata updated since its last sync.
 */
public class SyncMetadataRequest(
  /**
   * The context of this request.
   */
  @field:WireField(
    tag = 1,
    adapter = "cryptauthv2.RequestContext#ADAPTER",
    label = WireField.Label.OMIT_IDENTITY,
    schemaIndex = 0,
  )
  @JvmField
  public val context: RequestContext? = null,
  /**
   * Public key material intended for group use. Will either be created by the
   * device upon first joining, or will be existing group public key in its
   * possession.
   * May be discarded if device is joining an existing group that already has a
   * group key pair.
   */
  @field:WireField(
    tag = 3,
    adapter = "com.squareup.wire.ProtoAdapter#BYTES",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "groupPublicKey",
    schemaIndex = 1,
  )
  @JvmField
  public val group_public_key: ByteString = ByteString.EMPTY,
  /**
   * Device's metadata encrypted with the given group public key.
   */
  @field:WireField(
    tag = 4,
    adapter = "com.squareup.wire.ProtoAdapter#BYTES",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "encryptedMetadata",
    schemaIndex = 2,
  )
  @JvmField
  public val encrypted_metadata: ByteString = ByteString.EMPTY,
  /**
   * Set to true if the device needs the group's private key.
   */
  @field:WireField(
    tag = 5,
    adapter = "com.squareup.wire.ProtoAdapter#BOOL",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "needGroupPrivateKey",
    schemaIndex = 3,
  )
  @JvmField
  public val need_group_private_key: Boolean = false,
  /**
   * Used to obtain only updated metadata, since the last request.
   * If not set, all metadata will be sent.
   */
  @field:WireField(
    tag = 6,
    adapter = "com.squareup.wire.ProtoAdapter#BYTES",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "freshnessToken",
    schemaIndex = 4,
  )
  @JvmField
  public val freshness_token: ByteString = ByteString.EMPTY,
  unknownFields: ByteString = ByteString.EMPTY,
) : Message<SyncMetadataRequest, SyncMetadataRequest.Builder>(ADAPTER, unknownFields) {
  override fun newBuilder(): Builder {
    val builder = Builder()
    builder.context = context
    builder.group_public_key = group_public_key
    builder.encrypted_metadata = encrypted_metadata
    builder.need_group_private_key = need_group_private_key
    builder.freshness_token = freshness_token
    builder.addUnknownFields(unknownFields)
    return builder
  }

  override fun equals(other: Any?): Boolean {
    if (other === this) return true
    if (other !is SyncMetadataRequest) return false
    if (unknownFields != other.unknownFields) return false
    if (context != other.context) return false
    if (group_public_key != other.group_public_key) return false
    if (encrypted_metadata != other.encrypted_metadata) return false
    if (need_group_private_key != other.need_group_private_key) return false
    if (freshness_token != other.freshness_token) return false
    return true
  }

  override fun hashCode(): Int {
    var result = super.hashCode
    if (result == 0) {
      result = unknownFields.hashCode()
      result = result * 37 + (context?.hashCode() ?: 0)
      result = result * 37 + group_public_key.hashCode()
      result = result * 37 + encrypted_metadata.hashCode()
      result = result * 37 + need_group_private_key.hashCode()
      result = result * 37 + freshness_token.hashCode()
      super.hashCode = result
    }
    return result
  }

  override fun toString(): String {
    val result = mutableListOf<String>()
    if (context != null) result += """context=$context"""
    result += """group_public_key=$group_public_key"""
    result += """encrypted_metadata=$encrypted_metadata"""
    result += """need_group_private_key=$need_group_private_key"""
    result += """freshness_token=$freshness_token"""
    return result.joinToString(prefix = "SyncMetadataRequest{", separator = ", ", postfix = "}")
  }

  public fun copy(
    context: RequestContext? = this.context,
    group_public_key: ByteString = this.group_public_key,
    encrypted_metadata: ByteString = this.encrypted_metadata,
    need_group_private_key: Boolean = this.need_group_private_key,
    freshness_token: ByteString = this.freshness_token,
    unknownFields: ByteString = this.unknownFields,
  ): SyncMetadataRequest = SyncMetadataRequest(context, group_public_key, encrypted_metadata,
      need_group_private_key, freshness_token, unknownFields)

  public class Builder : Message.Builder<SyncMetadataRequest, Builder>() {
    @JvmField
    public var context: RequestContext? = null

    @JvmField
    public var group_public_key: ByteString = ByteString.EMPTY

    @JvmField
    public var encrypted_metadata: ByteString = ByteString.EMPTY

    @JvmField
    public var need_group_private_key: Boolean = false

    @JvmField
    public var freshness_token: ByteString = ByteString.EMPTY

    /**
     * The context of this request.
     */
    public fun context(context: RequestContext?): Builder {
      this.context = context
      return this
    }

    /**
     * Public key material intended for group use. Will either be created by the
     * device upon first joining, or will be existing group public key in its
     * possession.
     * May be discarded if device is joining an existing group that already has a
     * group key pair.
     */
    public fun group_public_key(group_public_key: ByteString): Builder {
      this.group_public_key = group_public_key
      return this
    }

    /**
     * Device's metadata encrypted with the given group public key.
     */
    public fun encrypted_metadata(encrypted_metadata: ByteString): Builder {
      this.encrypted_metadata = encrypted_metadata
      return this
    }

    /**
     * Set to true if the device needs the group's private key.
     */
    public fun need_group_private_key(need_group_private_key: Boolean): Builder {
      this.need_group_private_key = need_group_private_key
      return this
    }

    /**
     * Used to obtain only updated metadata, since the last request.
     * If not set, all metadata will be sent.
     */
    public fun freshness_token(freshness_token: ByteString): Builder {
      this.freshness_token = freshness_token
      return this
    }

    override fun build(): SyncMetadataRequest = SyncMetadataRequest(
      context = context,
      group_public_key = group_public_key,
      encrypted_metadata = encrypted_metadata,
      need_group_private_key = need_group_private_key,
      freshness_token = freshness_token,
      unknownFields = buildUnknownFields()
    )
  }

  public companion object {
    @JvmField
    public val ADAPTER: ProtoAdapter<SyncMetadataRequest> = object :
        ProtoAdapter<SyncMetadataRequest>(
      FieldEncoding.LENGTH_DELIMITED, 
      SyncMetadataRequest::class, 
      "type.googleapis.com/cryptauthv2.SyncMetadataRequest", 
      PROTO_3, 
      null, 
      "cryptauth/cryptauth_devicesync.proto"
    ) {
      override fun encodedSize(`value`: SyncMetadataRequest): Int {
        var size = value.unknownFields.size
        if (value.context != null) size += RequestContext.ADAPTER.encodedSizeWithTag(1,
            value.context)
        if (value.group_public_key != ByteString.EMPTY) size +=
            ProtoAdapter.BYTES.encodedSizeWithTag(3, value.group_public_key)
        if (value.encrypted_metadata != ByteString.EMPTY) size +=
            ProtoAdapter.BYTES.encodedSizeWithTag(4, value.encrypted_metadata)
        if (value.need_group_private_key != false) size += ProtoAdapter.BOOL.encodedSizeWithTag(5,
            value.need_group_private_key)
        if (value.freshness_token != ByteString.EMPTY) size +=
            ProtoAdapter.BYTES.encodedSizeWithTag(6, value.freshness_token)
        return size
      }

      override fun encode(writer: ProtoWriter, `value`: SyncMetadataRequest) {
        if (value.context != null) RequestContext.ADAPTER.encodeWithTag(writer, 1, value.context)
        if (value.group_public_key != ByteString.EMPTY) ProtoAdapter.BYTES.encodeWithTag(writer, 3,
            value.group_public_key)
        if (value.encrypted_metadata != ByteString.EMPTY) ProtoAdapter.BYTES.encodeWithTag(writer,
            4, value.encrypted_metadata)
        if (value.need_group_private_key != false) ProtoAdapter.BOOL.encodeWithTag(writer, 5,
            value.need_group_private_key)
        if (value.freshness_token != ByteString.EMPTY) ProtoAdapter.BYTES.encodeWithTag(writer, 6,
            value.freshness_token)
        writer.writeBytes(value.unknownFields)
      }

      override fun encode(writer: ReverseProtoWriter, `value`: SyncMetadataRequest) {
        writer.writeBytes(value.unknownFields)
        if (value.freshness_token != ByteString.EMPTY) ProtoAdapter.BYTES.encodeWithTag(writer, 6,
            value.freshness_token)
        if (value.need_group_private_key != false) ProtoAdapter.BOOL.encodeWithTag(writer, 5,
            value.need_group_private_key)
        if (value.encrypted_metadata != ByteString.EMPTY) ProtoAdapter.BYTES.encodeWithTag(writer,
            4, value.encrypted_metadata)
        if (value.group_public_key != ByteString.EMPTY) ProtoAdapter.BYTES.encodeWithTag(writer, 3,
            value.group_public_key)
        if (value.context != null) RequestContext.ADAPTER.encodeWithTag(writer, 1, value.context)
      }

      override fun decode(reader: ProtoReader): SyncMetadataRequest {
        var context: RequestContext? = null
        var group_public_key: ByteString = ByteString.EMPTY
        var encrypted_metadata: ByteString = ByteString.EMPTY
        var need_group_private_key: Boolean = false
        var freshness_token: ByteString = ByteString.EMPTY
        val unknownFields = reader.forEachTag { tag ->
          when (tag) {
            1 -> context = RequestContext.ADAPTER.decode(reader)
            3 -> group_public_key = ProtoAdapter.BYTES.decode(reader)
            4 -> encrypted_metadata = ProtoAdapter.BYTES.decode(reader)
            5 -> need_group_private_key = ProtoAdapter.BOOL.decode(reader)
            6 -> freshness_token = ProtoAdapter.BYTES.decode(reader)
            else -> reader.readUnknownField(tag)
          }
        }
        return SyncMetadataRequest(
          context = context,
          group_public_key = group_public_key,
          encrypted_metadata = encrypted_metadata,
          need_group_private_key = need_group_private_key,
          freshness_token = freshness_token,
          unknownFields = unknownFields
        )
      }

      override fun redact(`value`: SyncMetadataRequest): SyncMetadataRequest = value.copy(
        context = value.context?.let(RequestContext.ADAPTER::redact),
        unknownFields = ByteString.EMPTY
      )
    }

    private const val serialVersionUID: Long = 0L

    @JvmSynthetic
    public inline fun build(body: Builder.() -> Unit): SyncMetadataRequest =
        Builder().apply(body).build()
  }
}
