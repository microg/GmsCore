// Code generated by Wire protocol buffer compiler, do not edit.
// Source: cryptauthv2.InvokeNext in cryptauth/cryptauth_directive.proto
@file:Suppress("DEPRECATION")

package cryptauthv2

import com.squareup.wire.FieldEncoding
import com.squareup.wire.Message
import com.squareup.wire.ProtoAdapter
import com.squareup.wire.ProtoReader
import com.squareup.wire.ProtoWriter
import com.squareup.wire.ReverseProtoWriter
import com.squareup.wire.Syntax.PROTO_3
import com.squareup.wire.WireField
import com.squareup.wire.`internal`.JvmField
import com.squareup.wire.`internal`.JvmSynthetic
import com.squareup.wire.`internal`.sanitize
import kotlin.Any
import kotlin.Boolean
import kotlin.Int
import kotlin.Long
import kotlin.String
import kotlin.Suppress
import kotlin.Unit
import okio.ByteString

/**
 * Instructing the client to invoke a specific service.
 */
public class InvokeNext(
  /**
   * Target service to be involved next.
   */
  @field:WireField(
    tag = 1,
    adapter = "cryptauthv2.TargetService#ADAPTER",
    label = WireField.Label.OMIT_IDENTITY,
    schemaIndex = 0,
  )
  @JvmField
  public val service: TargetService = TargetService.TARGET_SERVICE_UNSPECIFIED,
  /**
   * Key name to be processed for target service.
   */
  @field:WireField(
    tag = 2,
    adapter = "com.squareup.wire.ProtoAdapter#STRING",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "keyName",
    schemaIndex = 1,
  )
  @JvmField
  public val key_name: String = "",
  unknownFields: ByteString = ByteString.EMPTY,
) : Message<InvokeNext, InvokeNext.Builder>(ADAPTER, unknownFields) {
  override fun newBuilder(): Builder {
    val builder = Builder()
    builder.service = service
    builder.key_name = key_name
    builder.addUnknownFields(unknownFields)
    return builder
  }

  override fun equals(other: Any?): Boolean {
    if (other === this) return true
    if (other !is InvokeNext) return false
    if (unknownFields != other.unknownFields) return false
    if (service != other.service) return false
    if (key_name != other.key_name) return false
    return true
  }

  override fun hashCode(): Int {
    var result = super.hashCode
    if (result == 0) {
      result = unknownFields.hashCode()
      result = result * 37 + service.hashCode()
      result = result * 37 + key_name.hashCode()
      super.hashCode = result
    }
    return result
  }

  override fun toString(): String {
    val result = mutableListOf<String>()
    result += """service=$service"""
    result += """key_name=${sanitize(key_name)}"""
    return result.joinToString(prefix = "InvokeNext{", separator = ", ", postfix = "}")
  }

  public fun copy(
    service: TargetService = this.service,
    key_name: String = this.key_name,
    unknownFields: ByteString = this.unknownFields,
  ): InvokeNext = InvokeNext(service, key_name, unknownFields)

  public class Builder : Message.Builder<InvokeNext, Builder>() {
    @JvmField
    public var service: TargetService = TargetService.TARGET_SERVICE_UNSPECIFIED

    @JvmField
    public var key_name: String = ""

    /**
     * Target service to be involved next.
     */
    public fun service(service: TargetService): Builder {
      this.service = service
      return this
    }

    /**
     * Key name to be processed for target service.
     */
    public fun key_name(key_name: String): Builder {
      this.key_name = key_name
      return this
    }

    override fun build(): InvokeNext = InvokeNext(
      service = service,
      key_name = key_name,
      unknownFields = buildUnknownFields()
    )
  }

  public companion object {
    @JvmField
    public val ADAPTER: ProtoAdapter<InvokeNext> = object : ProtoAdapter<InvokeNext>(
      FieldEncoding.LENGTH_DELIMITED, 
      InvokeNext::class, 
      "type.googleapis.com/cryptauthv2.InvokeNext", 
      PROTO_3, 
      null, 
      "cryptauth/cryptauth_directive.proto"
    ) {
      override fun encodedSize(`value`: InvokeNext): Int {
        var size = value.unknownFields.size
        if (value.service != TargetService.TARGET_SERVICE_UNSPECIFIED) size +=
            TargetService.ADAPTER.encodedSizeWithTag(1, value.service)
        if (value.key_name != "") size += ProtoAdapter.STRING.encodedSizeWithTag(2, value.key_name)
        return size
      }

      override fun encode(writer: ProtoWriter, `value`: InvokeNext) {
        if (value.service != TargetService.TARGET_SERVICE_UNSPECIFIED)
            TargetService.ADAPTER.encodeWithTag(writer, 1, value.service)
        if (value.key_name != "") ProtoAdapter.STRING.encodeWithTag(writer, 2, value.key_name)
        writer.writeBytes(value.unknownFields)
      }

      override fun encode(writer: ReverseProtoWriter, `value`: InvokeNext) {
        writer.writeBytes(value.unknownFields)
        if (value.key_name != "") ProtoAdapter.STRING.encodeWithTag(writer, 2, value.key_name)
        if (value.service != TargetService.TARGET_SERVICE_UNSPECIFIED)
            TargetService.ADAPTER.encodeWithTag(writer, 1, value.service)
      }

      override fun decode(reader: ProtoReader): InvokeNext {
        var service: TargetService = TargetService.TARGET_SERVICE_UNSPECIFIED
        var key_name: String = ""
        val unknownFields = reader.forEachTag { tag ->
          when (tag) {
            1 -> try {
              service = TargetService.ADAPTER.decode(reader)
            } catch (e: ProtoAdapter.EnumConstantNotFoundException) {
              reader.addUnknownField(tag, FieldEncoding.VARINT, e.value.toLong())
            }
            2 -> key_name = ProtoAdapter.STRING.decode(reader)
            else -> reader.readUnknownField(tag)
          }
        }
        return InvokeNext(
          service = service,
          key_name = key_name,
          unknownFields = unknownFields
        )
      }

      override fun redact(`value`: InvokeNext): InvokeNext = value.copy(
        unknownFields = ByteString.EMPTY
      )
    }

    private const val serialVersionUID: Long = 0L

    @JvmSynthetic
    public inline fun build(body: Builder.() -> Unit): InvokeNext = Builder().apply(body).build()
  }
}
