// Code generated by Wire protocol buffer compiler, do not edit.
// Source: cryptauthv2.DeviceActivityStatus in cryptauth/cryptauth_devicesync.proto
@file:Suppress("DEPRECATION")

package cryptauthv2

import com.squareup.wire.FieldEncoding
import com.squareup.wire.Message
import com.squareup.wire.ProtoAdapter
import com.squareup.wire.ProtoReader
import com.squareup.wire.ProtoWriter
import com.squareup.wire.ReverseProtoWriter
import com.squareup.wire.Syntax.PROTO_3
import com.squareup.wire.WireField
import com.squareup.wire.`internal`.JvmField
import com.squareup.wire.`internal`.JvmSynthetic
import com.squareup.wire.`internal`.sanitize
import kotlin.Any
import kotlin.Boolean
import kotlin.Int
import kotlin.Long
import kotlin.String
import kotlin.Suppress
import kotlin.Unit
import okio.ByteString

/**
 * Activity status of a single device.
 */
public class DeviceActivityStatus(
  /**
   * Device identifier.
   */
  @field:WireField(
    tag = 1,
    adapter = "com.squareup.wire.ProtoAdapter#STRING",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "deviceId",
    schemaIndex = 0,
  )
  @JvmField
  public val device_id: String = "",
  /**
   * The last time this device was active as retrieved from Bond. Freshness
   * is on the order of minutes.
   */
  @field:WireField(
    tag = 2,
    adapter = "com.squareup.wire.ProtoAdapter#INT64",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "lastActivityTimeSec",
    schemaIndex = 1,
  )
  @JvmField
  public val last_activity_time_sec: Long = 0L,
  /**
   * Online status of the device as inferred by reachability via FCM.
   */
  @field:WireField(
    tag = 3,
    adapter = "cryptauthv2.ConnectivityStatus#ADAPTER",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "connectivityStatus",
    schemaIndex = 2,
  )
  @JvmField
  public val connectivity_status: ConnectivityStatus = ConnectivityStatus.UNKNOWN_CONNECTIVITY,
  /**
   * When the device last enrolled its DeviceSync key or when its metadata was
   * last updated, the most recent of these two timestamps.
   */
  @field:WireField(
    tag = 4,
    adapter = "cryptauthv2.Timestamp#ADAPTER",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "lastUpdateTime",
    schemaIndex = 3,
  )
  @JvmField
  public val last_update_time: Timestamp? = null,
  unknownFields: ByteString = ByteString.EMPTY,
) : Message<DeviceActivityStatus, DeviceActivityStatus.Builder>(ADAPTER, unknownFields) {
  override fun newBuilder(): Builder {
    val builder = Builder()
    builder.device_id = device_id
    builder.last_activity_time_sec = last_activity_time_sec
    builder.connectivity_status = connectivity_status
    builder.last_update_time = last_update_time
    builder.addUnknownFields(unknownFields)
    return builder
  }

  override fun equals(other: Any?): Boolean {
    if (other === this) return true
    if (other !is DeviceActivityStatus) return false
    if (unknownFields != other.unknownFields) return false
    if (device_id != other.device_id) return false
    if (last_activity_time_sec != other.last_activity_time_sec) return false
    if (connectivity_status != other.connectivity_status) return false
    if (last_update_time != other.last_update_time) return false
    return true
  }

  override fun hashCode(): Int {
    var result = super.hashCode
    if (result == 0) {
      result = unknownFields.hashCode()
      result = result * 37 + device_id.hashCode()
      result = result * 37 + last_activity_time_sec.hashCode()
      result = result * 37 + connectivity_status.hashCode()
      result = result * 37 + (last_update_time?.hashCode() ?: 0)
      super.hashCode = result
    }
    return result
  }

  override fun toString(): String {
    val result = mutableListOf<String>()
    result += """device_id=${sanitize(device_id)}"""
    result += """last_activity_time_sec=$last_activity_time_sec"""
    result += """connectivity_status=$connectivity_status"""
    if (last_update_time != null) result += """last_update_time=$last_update_time"""
    return result.joinToString(prefix = "DeviceActivityStatus{", separator = ", ", postfix = "}")
  }

  public fun copy(
    device_id: String = this.device_id,
    last_activity_time_sec: Long = this.last_activity_time_sec,
    connectivity_status: ConnectivityStatus = this.connectivity_status,
    last_update_time: Timestamp? = this.last_update_time,
    unknownFields: ByteString = this.unknownFields,
  ): DeviceActivityStatus = DeviceActivityStatus(device_id, last_activity_time_sec,
      connectivity_status, last_update_time, unknownFields)

  public class Builder : Message.Builder<DeviceActivityStatus, Builder>() {
    @JvmField
    public var device_id: String = ""

    @JvmField
    public var last_activity_time_sec: Long = 0L

    @JvmField
    public var connectivity_status: ConnectivityStatus = ConnectivityStatus.UNKNOWN_CONNECTIVITY

    @JvmField
    public var last_update_time: Timestamp? = null

    /**
     * Device identifier.
     */
    public fun device_id(device_id: String): Builder {
      this.device_id = device_id
      return this
    }

    /**
     * The last time this device was active as retrieved from Bond. Freshness
     * is on the order of minutes.
     */
    public fun last_activity_time_sec(last_activity_time_sec: Long): Builder {
      this.last_activity_time_sec = last_activity_time_sec
      return this
    }

    /**
     * Online status of the device as inferred by reachability via FCM.
     */
    public fun connectivity_status(connectivity_status: ConnectivityStatus): Builder {
      this.connectivity_status = connectivity_status
      return this
    }

    /**
     * When the device last enrolled its DeviceSync key or when its metadata was
     * last updated, the most recent of these two timestamps.
     */
    public fun last_update_time(last_update_time: Timestamp?): Builder {
      this.last_update_time = last_update_time
      return this
    }

    override fun build(): DeviceActivityStatus = DeviceActivityStatus(
      device_id = device_id,
      last_activity_time_sec = last_activity_time_sec,
      connectivity_status = connectivity_status,
      last_update_time = last_update_time,
      unknownFields = buildUnknownFields()
    )
  }

  public companion object {
    @JvmField
    public val ADAPTER: ProtoAdapter<DeviceActivityStatus> = object :
        ProtoAdapter<DeviceActivityStatus>(
      FieldEncoding.LENGTH_DELIMITED, 
      DeviceActivityStatus::class, 
      "type.googleapis.com/cryptauthv2.DeviceActivityStatus", 
      PROTO_3, 
      null, 
      "cryptauth/cryptauth_devicesync.proto"
    ) {
      override fun encodedSize(`value`: DeviceActivityStatus): Int {
        var size = value.unknownFields.size
        if (value.device_id != "") size += ProtoAdapter.STRING.encodedSizeWithTag(1,
            value.device_id)
        if (value.last_activity_time_sec != 0L) size += ProtoAdapter.INT64.encodedSizeWithTag(2,
            value.last_activity_time_sec)
        if (value.connectivity_status != ConnectivityStatus.UNKNOWN_CONNECTIVITY) size +=
            ConnectivityStatus.ADAPTER.encodedSizeWithTag(3, value.connectivity_status)
        if (value.last_update_time != null) size += Timestamp.ADAPTER.encodedSizeWithTag(4,
            value.last_update_time)
        return size
      }

      override fun encode(writer: ProtoWriter, `value`: DeviceActivityStatus) {
        if (value.device_id != "") ProtoAdapter.STRING.encodeWithTag(writer, 1, value.device_id)
        if (value.last_activity_time_sec != 0L) ProtoAdapter.INT64.encodeWithTag(writer, 2,
            value.last_activity_time_sec)
        if (value.connectivity_status != ConnectivityStatus.UNKNOWN_CONNECTIVITY)
            ConnectivityStatus.ADAPTER.encodeWithTag(writer, 3, value.connectivity_status)
        if (value.last_update_time != null) Timestamp.ADAPTER.encodeWithTag(writer, 4,
            value.last_update_time)
        writer.writeBytes(value.unknownFields)
      }

      override fun encode(writer: ReverseProtoWriter, `value`: DeviceActivityStatus) {
        writer.writeBytes(value.unknownFields)
        if (value.last_update_time != null) Timestamp.ADAPTER.encodeWithTag(writer, 4,
            value.last_update_time)
        if (value.connectivity_status != ConnectivityStatus.UNKNOWN_CONNECTIVITY)
            ConnectivityStatus.ADAPTER.encodeWithTag(writer, 3, value.connectivity_status)
        if (value.last_activity_time_sec != 0L) ProtoAdapter.INT64.encodeWithTag(writer, 2,
            value.last_activity_time_sec)
        if (value.device_id != "") ProtoAdapter.STRING.encodeWithTag(writer, 1, value.device_id)
      }

      override fun decode(reader: ProtoReader): DeviceActivityStatus {
        var device_id: String = ""
        var last_activity_time_sec: Long = 0L
        var connectivity_status: ConnectivityStatus = ConnectivityStatus.UNKNOWN_CONNECTIVITY
        var last_update_time: Timestamp? = null
        val unknownFields = reader.forEachTag { tag ->
          when (tag) {
            1 -> device_id = ProtoAdapter.STRING.decode(reader)
            2 -> last_activity_time_sec = ProtoAdapter.INT64.decode(reader)
            3 -> try {
              connectivity_status = ConnectivityStatus.ADAPTER.decode(reader)
            } catch (e: ProtoAdapter.EnumConstantNotFoundException) {
              reader.addUnknownField(tag, FieldEncoding.VARINT, e.value.toLong())
            }
            4 -> last_update_time = Timestamp.ADAPTER.decode(reader)
            else -> reader.readUnknownField(tag)
          }
        }
        return DeviceActivityStatus(
          device_id = device_id,
          last_activity_time_sec = last_activity_time_sec,
          connectivity_status = connectivity_status,
          last_update_time = last_update_time,
          unknownFields = unknownFields
        )
      }

      override fun redact(`value`: DeviceActivityStatus): DeviceActivityStatus = value.copy(
        last_update_time = value.last_update_time?.let(Timestamp.ADAPTER::redact),
        unknownFields = ByteString.EMPTY
      )
    }

    private const val serialVersionUID: Long = 0L

    @JvmSynthetic
    public inline fun build(body: Builder.() -> Unit): DeviceActivityStatus =
        Builder().apply(body).build()
  }
}
