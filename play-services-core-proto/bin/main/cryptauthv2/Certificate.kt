// Code generated by Wire protocol buffer compiler, do not edit.
// Source: cryptauthv2.Certificate in cryptauth/cryptauth_common.proto
@file:Suppress("DEPRECATION")

package cryptauthv2

import com.squareup.wire.FieldEncoding
import com.squareup.wire.Message
import com.squareup.wire.ProtoAdapter
import com.squareup.wire.ProtoReader
import com.squareup.wire.ProtoWriter
import com.squareup.wire.ReverseProtoWriter
import com.squareup.wire.Syntax.PROTO_3
import com.squareup.wire.WireField
import com.squareup.wire.`internal`.JvmField
import com.squareup.wire.`internal`.JvmSynthetic
import com.squareup.wire.`internal`.checkElementsNotNull
import com.squareup.wire.`internal`.immutableCopyOf
import com.squareup.wire.`internal`.redactElements
import com.squareup.wire.`internal`.sanitize
import kotlin.Any
import kotlin.Boolean
import kotlin.Int
import kotlin.Long
import kotlin.String
import kotlin.Suppress
import kotlin.Unit
import kotlin.collections.List
import okio.ByteString

/**
 * The generic format for public-key certificates.
 */
public class Certificate(
  /**
   * The identifier bound to the cert, e.g., an email address or phone number.
   */
  @field:WireField(
    tag = 1,
    adapter = "com.squareup.wire.ProtoAdapter#STRING",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "commonName",
    schemaIndex = 0,
  )
  @JvmField
  public val common_name: String = "",
  /**
   * The raw bytes of the public key.
   */
  @field:WireField(
    tag = 2,
    adapter = "com.squareup.wire.ProtoAdapter#BYTES",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "publicKey",
    schemaIndex = 1,
  )
  @JvmField
  public val public_key: ByteString = ByteString.EMPTY,
  /**
   * The UNIX timestamp when the cert will expire.
   */
  @field:WireField(
    tag = 3,
    adapter = "com.squareup.wire.ProtoAdapter#INT64",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "expireTimeMillis",
    schemaIndex = 2,
  )
  @JvmField
  public val expire_time_millis: Long = 0L,
  claims: List<Claim> = emptyList(),
  /**
   * The signature over all of the above.
   */
  @field:WireField(
    tag = 5,
    adapter = "com.squareup.wire.ProtoAdapter#BYTES",
    label = WireField.Label.OMIT_IDENTITY,
    schemaIndex = 4,
  )
  @JvmField
  public val signature: ByteString = ByteString.EMPTY,
  unknownFields: ByteString = ByteString.EMPTY,
) : Message<Certificate, Certificate.Builder>(ADAPTER, unknownFields) {
  /**
   * All claims associated with the use of this key.
   */
  @field:WireField(
    tag = 4,
    adapter = "cryptauthv2.Certificate${'$'}Claim#ADAPTER",
    label = WireField.Label.REPEATED,
    schemaIndex = 3,
  )
  @JvmField
  public val claims: List<Claim> = immutableCopyOf("claims", claims)

  override fun newBuilder(): Builder {
    val builder = Builder()
    builder.common_name = common_name
    builder.public_key = public_key
    builder.expire_time_millis = expire_time_millis
    builder.claims = claims
    builder.signature = signature
    builder.addUnknownFields(unknownFields)
    return builder
  }

  override fun equals(other: Any?): Boolean {
    if (other === this) return true
    if (other !is Certificate) return false
    if (unknownFields != other.unknownFields) return false
    if (common_name != other.common_name) return false
    if (public_key != other.public_key) return false
    if (expire_time_millis != other.expire_time_millis) return false
    if (claims != other.claims) return false
    if (signature != other.signature) return false
    return true
  }

  override fun hashCode(): Int {
    var result = super.hashCode
    if (result == 0) {
      result = unknownFields.hashCode()
      result = result * 37 + common_name.hashCode()
      result = result * 37 + public_key.hashCode()
      result = result * 37 + expire_time_millis.hashCode()
      result = result * 37 + claims.hashCode()
      result = result * 37 + signature.hashCode()
      super.hashCode = result
    }
    return result
  }

  override fun toString(): String {
    val result = mutableListOf<String>()
    result += """common_name=${sanitize(common_name)}"""
    result += """public_key=$public_key"""
    result += """expire_time_millis=$expire_time_millis"""
    if (claims.isNotEmpty()) result += """claims=$claims"""
    result += """signature=$signature"""
    return result.joinToString(prefix = "Certificate{", separator = ", ", postfix = "}")
  }

  public fun copy(
    common_name: String = this.common_name,
    public_key: ByteString = this.public_key,
    expire_time_millis: Long = this.expire_time_millis,
    claims: List<Claim> = this.claims,
    signature: ByteString = this.signature,
    unknownFields: ByteString = this.unknownFields,
  ): Certificate = Certificate(common_name, public_key, expire_time_millis, claims, signature,
      unknownFields)

  public class Builder : Message.Builder<Certificate, Builder>() {
    @JvmField
    public var common_name: String = ""

    @JvmField
    public var public_key: ByteString = ByteString.EMPTY

    @JvmField
    public var expire_time_millis: Long = 0L

    @JvmField
    public var claims: List<Claim> = emptyList()

    @JvmField
    public var signature: ByteString = ByteString.EMPTY

    /**
     * The identifier bound to the cert, e.g., an email address or phone number.
     */
    public fun common_name(common_name: String): Builder {
      this.common_name = common_name
      return this
    }

    /**
     * The raw bytes of the public key.
     */
    public fun public_key(public_key: ByteString): Builder {
      this.public_key = public_key
      return this
    }

    /**
     * The UNIX timestamp when the cert will expire.
     */
    public fun expire_time_millis(expire_time_millis: Long): Builder {
      this.expire_time_millis = expire_time_millis
      return this
    }

    /**
     * All claims associated with the use of this key.
     */
    public fun claims(claims: List<Claim>): Builder {
      checkElementsNotNull(claims)
      this.claims = claims
      return this
    }

    /**
     * The signature over all of the above.
     */
    public fun signature(signature: ByteString): Builder {
      this.signature = signature
      return this
    }

    override fun build(): Certificate = Certificate(
      common_name = common_name,
      public_key = public_key,
      expire_time_millis = expire_time_millis,
      claims = claims,
      signature = signature,
      unknownFields = buildUnknownFields()
    )
  }

  public companion object {
    @JvmField
    public val ADAPTER: ProtoAdapter<Certificate> = object : ProtoAdapter<Certificate>(
      FieldEncoding.LENGTH_DELIMITED, 
      Certificate::class, 
      "type.googleapis.com/cryptauthv2.Certificate", 
      PROTO_3, 
      null, 
      "cryptauth/cryptauth_common.proto"
    ) {
      override fun encodedSize(`value`: Certificate): Int {
        var size = value.unknownFields.size
        if (value.common_name != "") size += ProtoAdapter.STRING.encodedSizeWithTag(1,
            value.common_name)
        if (value.public_key != ByteString.EMPTY) size += ProtoAdapter.BYTES.encodedSizeWithTag(2,
            value.public_key)
        if (value.expire_time_millis != 0L) size += ProtoAdapter.INT64.encodedSizeWithTag(3,
            value.expire_time_millis)
        size += Claim.ADAPTER.asRepeated().encodedSizeWithTag(4, value.claims)
        if (value.signature != ByteString.EMPTY) size += ProtoAdapter.BYTES.encodedSizeWithTag(5,
            value.signature)
        return size
      }

      override fun encode(writer: ProtoWriter, `value`: Certificate) {
        if (value.common_name != "") ProtoAdapter.STRING.encodeWithTag(writer, 1, value.common_name)
        if (value.public_key != ByteString.EMPTY) ProtoAdapter.BYTES.encodeWithTag(writer, 2,
            value.public_key)
        if (value.expire_time_millis != 0L) ProtoAdapter.INT64.encodeWithTag(writer, 3,
            value.expire_time_millis)
        Claim.ADAPTER.asRepeated().encodeWithTag(writer, 4, value.claims)
        if (value.signature != ByteString.EMPTY) ProtoAdapter.BYTES.encodeWithTag(writer, 5,
            value.signature)
        writer.writeBytes(value.unknownFields)
      }

      override fun encode(writer: ReverseProtoWriter, `value`: Certificate) {
        writer.writeBytes(value.unknownFields)
        if (value.signature != ByteString.EMPTY) ProtoAdapter.BYTES.encodeWithTag(writer, 5,
            value.signature)
        Claim.ADAPTER.asRepeated().encodeWithTag(writer, 4, value.claims)
        if (value.expire_time_millis != 0L) ProtoAdapter.INT64.encodeWithTag(writer, 3,
            value.expire_time_millis)
        if (value.public_key != ByteString.EMPTY) ProtoAdapter.BYTES.encodeWithTag(writer, 2,
            value.public_key)
        if (value.common_name != "") ProtoAdapter.STRING.encodeWithTag(writer, 1, value.common_name)
      }

      override fun decode(reader: ProtoReader): Certificate {
        var common_name: String = ""
        var public_key: ByteString = ByteString.EMPTY
        var expire_time_millis: Long = 0L
        val claims = mutableListOf<Claim>()
        var signature: ByteString = ByteString.EMPTY
        val unknownFields = reader.forEachTag { tag ->
          when (tag) {
            1 -> common_name = ProtoAdapter.STRING.decode(reader)
            2 -> public_key = ProtoAdapter.BYTES.decode(reader)
            3 -> expire_time_millis = ProtoAdapter.INT64.decode(reader)
            4 -> claims.add(Claim.ADAPTER.decode(reader))
            5 -> signature = ProtoAdapter.BYTES.decode(reader)
            else -> reader.readUnknownField(tag)
          }
        }
        return Certificate(
          common_name = common_name,
          public_key = public_key,
          expire_time_millis = expire_time_millis,
          claims = claims,
          signature = signature,
          unknownFields = unknownFields
        )
      }

      override fun redact(`value`: Certificate): Certificate = value.copy(
        claims = value.claims.redactElements(Claim.ADAPTER),
        unknownFields = ByteString.EMPTY
      )
    }

    private const val serialVersionUID: Long = 0L

    @JvmSynthetic
    public inline fun build(body: Builder.() -> Unit): Certificate = Builder().apply(body).build()
  }

  /**
   * A restriction imposed on the applications using this key.
   * Claims are validated along with the signature, when this key is used.
   */
  public class Claim(
    /**
     * Claim name.
     */
    @field:WireField(
      tag = 1,
      adapter = "com.squareup.wire.ProtoAdapter#STRING",
      label = WireField.Label.OMIT_IDENTITY,
      schemaIndex = 0,
    )
    @JvmField
    public val name: String = "",
    /**
     * Whether this claim is critical in the certificate. If it is critical,
     * the client must fail the validation of the certificate if the client does
     * not recognize the name of the claim.
     */
    @field:WireField(
      tag = 2,
      adapter = "com.squareup.wire.ProtoAdapter#BOOL",
      label = WireField.Label.OMIT_IDENTITY,
      schemaIndex = 1,
    )
    @JvmField
    public val critical: Boolean = false,
    /**
     * Claim value.
     */
    @field:WireField(
      tag = 3,
      adapter = "com.squareup.wire.ProtoAdapter#BYTES",
      label = WireField.Label.OMIT_IDENTITY,
      declaredName = "value",
      schemaIndex = 2,
    )
    @JvmField
    public val value_: ByteString = ByteString.EMPTY,
    unknownFields: ByteString = ByteString.EMPTY,
  ) : Message<Claim, Claim.Builder>(ADAPTER, unknownFields) {
    override fun newBuilder(): Builder {
      val builder = Builder()
      builder.name = name
      builder.critical = critical
      builder.value_ = value_
      builder.addUnknownFields(unknownFields)
      return builder
    }

    override fun equals(other: Any?): Boolean {
      if (other === this) return true
      if (other !is Claim) return false
      if (unknownFields != other.unknownFields) return false
      if (name != other.name) return false
      if (critical != other.critical) return false
      if (value_ != other.value_) return false
      return true
    }

    override fun hashCode(): Int {
      var result = super.hashCode
      if (result == 0) {
        result = unknownFields.hashCode()
        result = result * 37 + name.hashCode()
        result = result * 37 + critical.hashCode()
        result = result * 37 + value_.hashCode()
        super.hashCode = result
      }
      return result
    }

    override fun toString(): String {
      val result = mutableListOf<String>()
      result += """name=${sanitize(name)}"""
      result += """critical=$critical"""
      result += """value_=$value_"""
      return result.joinToString(prefix = "Claim{", separator = ", ", postfix = "}")
    }

    public fun copy(
      name: String = this.name,
      critical: Boolean = this.critical,
      value_: ByteString = this.value_,
      unknownFields: ByteString = this.unknownFields,
    ): Claim = Claim(name, critical, value_, unknownFields)

    public class Builder : Message.Builder<Claim, Builder>() {
      @JvmField
      public var name: String = ""

      @JvmField
      public var critical: Boolean = false

      @JvmField
      public var value_: ByteString = ByteString.EMPTY

      /**
       * Claim name.
       */
      public fun name(name: String): Builder {
        this.name = name
        return this
      }

      /**
       * Whether this claim is critical in the certificate. If it is critical,
       * the client must fail the validation of the certificate if the client does
       * not recognize the name of the claim.
       */
      public fun critical(critical: Boolean): Builder {
        this.critical = critical
        return this
      }

      /**
       * Claim value.
       */
      public fun value_(value_: ByteString): Builder {
        this.value_ = value_
        return this
      }

      override fun build(): Claim = Claim(
        name = name,
        critical = critical,
        value_ = value_,
        unknownFields = buildUnknownFields()
      )
    }

    public companion object {
      @JvmField
      public val ADAPTER: ProtoAdapter<Claim> = object : ProtoAdapter<Claim>(
        FieldEncoding.LENGTH_DELIMITED, 
        Claim::class, 
        "type.googleapis.com/cryptauthv2.Certificate.Claim", 
        PROTO_3, 
        null, 
        "cryptauth/cryptauth_common.proto"
      ) {
        override fun encodedSize(`value`: Claim): Int {
          var size = value.unknownFields.size
          if (value.name != "") size += ProtoAdapter.STRING.encodedSizeWithTag(1, value.name)
          if (value.critical != false) size += ProtoAdapter.BOOL.encodedSizeWithTag(2,
              value.critical)
          if (value.value_ != ByteString.EMPTY) size += ProtoAdapter.BYTES.encodedSizeWithTag(3,
              value.value_)
          return size
        }

        override fun encode(writer: ProtoWriter, `value`: Claim) {
          if (value.name != "") ProtoAdapter.STRING.encodeWithTag(writer, 1, value.name)
          if (value.critical != false) ProtoAdapter.BOOL.encodeWithTag(writer, 2, value.critical)
          if (value.value_ != ByteString.EMPTY) ProtoAdapter.BYTES.encodeWithTag(writer, 3,
              value.value_)
          writer.writeBytes(value.unknownFields)
        }

        override fun encode(writer: ReverseProtoWriter, `value`: Claim) {
          writer.writeBytes(value.unknownFields)
          if (value.value_ != ByteString.EMPTY) ProtoAdapter.BYTES.encodeWithTag(writer, 3,
              value.value_)
          if (value.critical != false) ProtoAdapter.BOOL.encodeWithTag(writer, 2, value.critical)
          if (value.name != "") ProtoAdapter.STRING.encodeWithTag(writer, 1, value.name)
        }

        override fun decode(reader: ProtoReader): Claim {
          var name: String = ""
          var critical: Boolean = false
          var value_: ByteString = ByteString.EMPTY
          val unknownFields = reader.forEachTag { tag ->
            when (tag) {
              1 -> name = ProtoAdapter.STRING.decode(reader)
              2 -> critical = ProtoAdapter.BOOL.decode(reader)
              3 -> value_ = ProtoAdapter.BYTES.decode(reader)
              else -> reader.readUnknownField(tag)
            }
          }
          return Claim(
            name = name,
            critical = critical,
            value_ = value_,
            unknownFields = unknownFields
          )
        }

        override fun redact(`value`: Claim): Claim = value.copy(
          unknownFields = ByteString.EMPTY
        )
      }

      private const val serialVersionUID: Long = 0L

      @JvmSynthetic
      public inline fun build(body: Builder.() -> Unit): Claim = Builder().apply(body).build()
    }
  }
}
