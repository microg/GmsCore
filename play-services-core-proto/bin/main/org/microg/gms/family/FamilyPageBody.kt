// Code generated by Wire protocol buffer compiler, do not edit.
// Source: google.familymanagement.v1.FamilyPageBody in familymanagement.proto
@file:Suppress("DEPRECATION")

package org.microg.gms.family

import com.squareup.wire.FieldEncoding
import com.squareup.wire.Message
import com.squareup.wire.ProtoAdapter
import com.squareup.wire.ProtoReader
import com.squareup.wire.ProtoWriter
import com.squareup.wire.ReverseProtoWriter
import com.squareup.wire.Syntax.PROTO_2
import com.squareup.wire.WireField
import com.squareup.wire.`internal`.JvmField
import com.squareup.wire.`internal`.JvmSynthetic
import com.squareup.wire.`internal`.checkElementsNotNull
import com.squareup.wire.`internal`.immutableCopyOf
import com.squareup.wire.`internal`.redactElements
import kotlin.Any
import kotlin.Boolean
import kotlin.Int
import kotlin.Long
import kotlin.String
import kotlin.Suppress
import kotlin.Unit
import kotlin.collections.List
import okio.ByteString

public class FamilyPageBody(
  @field:WireField(
    tag = 1,
    adapter = "com.squareup.wire.ProtoAdapter#INT32",
    schemaIndex = 0,
  )
  @JvmField
  public val status: Int? = null,
  sections: List<FamilySection> = emptyList(),
  helpLinks: List<FamilyHelpLink> = emptyList(),
  bulletPoints: List<FamilyBulletPoint> = emptyList(),
  unknownFields: ByteString = ByteString.EMPTY,
) : Message<FamilyPageBody, FamilyPageBody.Builder>(ADAPTER, unknownFields) {
  @field:WireField(
    tag = 2,
    adapter = "org.microg.gms.family.FamilySection#ADAPTER",
    label = WireField.Label.REPEATED,
    schemaIndex = 1,
  )
  @JvmField
  public val sections: List<FamilySection> = immutableCopyOf("sections", sections)

  @field:WireField(
    tag = 3,
    adapter = "org.microg.gms.family.FamilyHelpLink#ADAPTER",
    label = WireField.Label.REPEATED,
    schemaIndex = 2,
  )
  @JvmField
  public val helpLinks: List<FamilyHelpLink> = immutableCopyOf("helpLinks", helpLinks)

  @field:WireField(
    tag = 4,
    adapter = "org.microg.gms.family.FamilyBulletPoint#ADAPTER",
    label = WireField.Label.REPEATED,
    schemaIndex = 3,
  )
  @JvmField
  public val bulletPoints: List<FamilyBulletPoint> = immutableCopyOf("bulletPoints", bulletPoints)

  override fun newBuilder(): Builder {
    val builder = Builder()
    builder.status = status
    builder.sections = sections
    builder.helpLinks = helpLinks
    builder.bulletPoints = bulletPoints
    builder.addUnknownFields(unknownFields)
    return builder
  }

  override fun equals(other: Any?): Boolean {
    if (other === this) return true
    if (other !is FamilyPageBody) return false
    if (unknownFields != other.unknownFields) return false
    if (status != other.status) return false
    if (sections != other.sections) return false
    if (helpLinks != other.helpLinks) return false
    if (bulletPoints != other.bulletPoints) return false
    return true
  }

  override fun hashCode(): Int {
    var result = super.hashCode
    if (result == 0) {
      result = unknownFields.hashCode()
      result = result * 37 + (status?.hashCode() ?: 0)
      result = result * 37 + sections.hashCode()
      result = result * 37 + helpLinks.hashCode()
      result = result * 37 + bulletPoints.hashCode()
      super.hashCode = result
    }
    return result
  }

  override fun toString(): String {
    val result = mutableListOf<String>()
    if (status != null) result += """status=$status"""
    if (sections.isNotEmpty()) result += """sections=$sections"""
    if (helpLinks.isNotEmpty()) result += """helpLinks=$helpLinks"""
    if (bulletPoints.isNotEmpty()) result += """bulletPoints=$bulletPoints"""
    return result.joinToString(prefix = "FamilyPageBody{", separator = ", ", postfix = "}")
  }

  public fun copy(
    status: Int? = this.status,
    sections: List<FamilySection> = this.sections,
    helpLinks: List<FamilyHelpLink> = this.helpLinks,
    bulletPoints: List<FamilyBulletPoint> = this.bulletPoints,
    unknownFields: ByteString = this.unknownFields,
  ): FamilyPageBody = FamilyPageBody(status, sections, helpLinks, bulletPoints, unknownFields)

  public class Builder : Message.Builder<FamilyPageBody, Builder>() {
    @JvmField
    public var status: Int? = null

    @JvmField
    public var sections: List<FamilySection> = emptyList()

    @JvmField
    public var helpLinks: List<FamilyHelpLink> = emptyList()

    @JvmField
    public var bulletPoints: List<FamilyBulletPoint> = emptyList()

    public fun status(status: Int?): Builder {
      this.status = status
      return this
    }

    public fun sections(sections: List<FamilySection>): Builder {
      checkElementsNotNull(sections)
      this.sections = sections
      return this
    }

    public fun helpLinks(helpLinks: List<FamilyHelpLink>): Builder {
      checkElementsNotNull(helpLinks)
      this.helpLinks = helpLinks
      return this
    }

    public fun bulletPoints(bulletPoints: List<FamilyBulletPoint>): Builder {
      checkElementsNotNull(bulletPoints)
      this.bulletPoints = bulletPoints
      return this
    }

    override fun build(): FamilyPageBody = FamilyPageBody(
      status = status,
      sections = sections,
      helpLinks = helpLinks,
      bulletPoints = bulletPoints,
      unknownFields = buildUnknownFields()
    )
  }

  public companion object {
    @JvmField
    public val ADAPTER: ProtoAdapter<FamilyPageBody> = object : ProtoAdapter<FamilyPageBody>(
      FieldEncoding.LENGTH_DELIMITED, 
      FamilyPageBody::class, 
      "type.googleapis.com/google.familymanagement.v1.FamilyPageBody", 
      PROTO_2, 
      null, 
      "familymanagement.proto"
    ) {
      override fun encodedSize(`value`: FamilyPageBody): Int {
        var size = value.unknownFields.size
        size += ProtoAdapter.INT32.encodedSizeWithTag(1, value.status)
        size += FamilySection.ADAPTER.asRepeated().encodedSizeWithTag(2, value.sections)
        size += FamilyHelpLink.ADAPTER.asRepeated().encodedSizeWithTag(3, value.helpLinks)
        size += FamilyBulletPoint.ADAPTER.asRepeated().encodedSizeWithTag(4, value.bulletPoints)
        return size
      }

      override fun encode(writer: ProtoWriter, `value`: FamilyPageBody) {
        ProtoAdapter.INT32.encodeWithTag(writer, 1, value.status)
        FamilySection.ADAPTER.asRepeated().encodeWithTag(writer, 2, value.sections)
        FamilyHelpLink.ADAPTER.asRepeated().encodeWithTag(writer, 3, value.helpLinks)
        FamilyBulletPoint.ADAPTER.asRepeated().encodeWithTag(writer, 4, value.bulletPoints)
        writer.writeBytes(value.unknownFields)
      }

      override fun encode(writer: ReverseProtoWriter, `value`: FamilyPageBody) {
        writer.writeBytes(value.unknownFields)
        FamilyBulletPoint.ADAPTER.asRepeated().encodeWithTag(writer, 4, value.bulletPoints)
        FamilyHelpLink.ADAPTER.asRepeated().encodeWithTag(writer, 3, value.helpLinks)
        FamilySection.ADAPTER.asRepeated().encodeWithTag(writer, 2, value.sections)
        ProtoAdapter.INT32.encodeWithTag(writer, 1, value.status)
      }

      override fun decode(reader: ProtoReader): FamilyPageBody {
        var status: Int? = null
        val sections = mutableListOf<FamilySection>()
        val helpLinks = mutableListOf<FamilyHelpLink>()
        val bulletPoints = mutableListOf<FamilyBulletPoint>()
        val unknownFields = reader.forEachTag { tag ->
          when (tag) {
            1 -> status = ProtoAdapter.INT32.decode(reader)
            2 -> sections.add(FamilySection.ADAPTER.decode(reader))
            3 -> helpLinks.add(FamilyHelpLink.ADAPTER.decode(reader))
            4 -> bulletPoints.add(FamilyBulletPoint.ADAPTER.decode(reader))
            else -> reader.readUnknownField(tag)
          }
        }
        return FamilyPageBody(
          status = status,
          sections = sections,
          helpLinks = helpLinks,
          bulletPoints = bulletPoints,
          unknownFields = unknownFields
        )
      }

      override fun redact(`value`: FamilyPageBody): FamilyPageBody = value.copy(
        sections = value.sections.redactElements(FamilySection.ADAPTER),
        helpLinks = value.helpLinks.redactElements(FamilyHelpLink.ADAPTER),
        bulletPoints = value.bulletPoints.redactElements(FamilyBulletPoint.ADAPTER),
        unknownFields = ByteString.EMPTY
      )
    }

    private const val serialVersionUID: Long = 0L

    @JvmSynthetic
    public inline fun build(body: Builder.() -> Unit): FamilyPageBody =
        Builder().apply(body).build()
  }
}
