// Code generated by Wire protocol buffer compiler, do not edit.
// Source: google.familymanagement.v1.FamilyMember in familymanagement.proto
@file:Suppress("DEPRECATION")

package org.microg.gms.family

import com.squareup.wire.FieldEncoding
import com.squareup.wire.Message
import com.squareup.wire.ProtoAdapter
import com.squareup.wire.ProtoReader
import com.squareup.wire.ProtoWriter
import com.squareup.wire.ReverseProtoWriter
import com.squareup.wire.Syntax.PROTO_2
import com.squareup.wire.WireField
import com.squareup.wire.`internal`.JvmField
import com.squareup.wire.`internal`.JvmSynthetic
import com.squareup.wire.`internal`.checkElementsNotNull
import com.squareup.wire.`internal`.immutableCopyOf
import com.squareup.wire.`internal`.sanitize
import kotlin.Any
import kotlin.Boolean
import kotlin.Int
import kotlin.Long
import kotlin.String
import kotlin.Suppress
import kotlin.Unit
import kotlin.collections.List
import okio.ByteString

public class FamilyMember(
  @field:WireField(
    tag = 1,
    adapter = "com.squareup.wire.ProtoAdapter#STRING",
    schemaIndex = 0,
  )
  @JvmField
  public val memberId: String? = null,
  @field:WireField(
    tag = 3,
    adapter = "org.microg.gms.family.FamilyRole#ADAPTER",
    schemaIndex = 1,
  )
  @JvmField
  public val role: FamilyRole? = null,
  @field:WireField(
    tag = 4,
    adapter = "org.microg.gms.family.MemberProfile#ADAPTER",
    schemaIndex = 2,
  )
  @JvmField
  public val profile: MemberProfile? = null,
  @field:WireField(
    tag = 6,
    adapter = "com.squareup.wire.ProtoAdapter#STRING",
    schemaIndex = 3,
  )
  @JvmField
  public val hohGivenName: String? = null,
  tags: List<Int> = emptyList(),
  unknownFields: ByteString = ByteString.EMPTY,
) : Message<FamilyMember, FamilyMember.Builder>(ADAPTER, unknownFields) {
  @field:WireField(
    tag = 7,
    adapter = "com.squareup.wire.ProtoAdapter#INT32",
    label = WireField.Label.REPEATED,
    schemaIndex = 4,
  )
  @JvmField
  public val tags: List<Int> = immutableCopyOf("tags", tags)

  override fun newBuilder(): Builder {
    val builder = Builder()
    builder.memberId = memberId
    builder.role = role
    builder.profile = profile
    builder.hohGivenName = hohGivenName
    builder.tags = tags
    builder.addUnknownFields(unknownFields)
    return builder
  }

  override fun equals(other: Any?): Boolean {
    if (other === this) return true
    if (other !is FamilyMember) return false
    if (unknownFields != other.unknownFields) return false
    if (memberId != other.memberId) return false
    if (role != other.role) return false
    if (profile != other.profile) return false
    if (hohGivenName != other.hohGivenName) return false
    if (tags != other.tags) return false
    return true
  }

  override fun hashCode(): Int {
    var result = super.hashCode
    if (result == 0) {
      result = unknownFields.hashCode()
      result = result * 37 + (memberId?.hashCode() ?: 0)
      result = result * 37 + (role?.hashCode() ?: 0)
      result = result * 37 + (profile?.hashCode() ?: 0)
      result = result * 37 + (hohGivenName?.hashCode() ?: 0)
      result = result * 37 + tags.hashCode()
      super.hashCode = result
    }
    return result
  }

  override fun toString(): String {
    val result = mutableListOf<String>()
    if (memberId != null) result += """memberId=${sanitize(memberId)}"""
    if (role != null) result += """role=$role"""
    if (profile != null) result += """profile=$profile"""
    if (hohGivenName != null) result += """hohGivenName=${sanitize(hohGivenName)}"""
    if (tags.isNotEmpty()) result += """tags=$tags"""
    return result.joinToString(prefix = "FamilyMember{", separator = ", ", postfix = "}")
  }

  public fun copy(
    memberId: String? = this.memberId,
    role: FamilyRole? = this.role,
    profile: MemberProfile? = this.profile,
    hohGivenName: String? = this.hohGivenName,
    tags: List<Int> = this.tags,
    unknownFields: ByteString = this.unknownFields,
  ): FamilyMember = FamilyMember(memberId, role, profile, hohGivenName, tags, unknownFields)

  public class Builder : Message.Builder<FamilyMember, Builder>() {
    @JvmField
    public var memberId: String? = null

    @JvmField
    public var role: FamilyRole? = null

    @JvmField
    public var profile: MemberProfile? = null

    @JvmField
    public var hohGivenName: String? = null

    @JvmField
    public var tags: List<Int> = emptyList()

    public fun memberId(memberId: String?): Builder {
      this.memberId = memberId
      return this
    }

    public fun role(role: FamilyRole?): Builder {
      this.role = role
      return this
    }

    public fun profile(profile: MemberProfile?): Builder {
      this.profile = profile
      return this
    }

    public fun hohGivenName(hohGivenName: String?): Builder {
      this.hohGivenName = hohGivenName
      return this
    }

    public fun tags(tags: List<Int>): Builder {
      checkElementsNotNull(tags)
      this.tags = tags
      return this
    }

    override fun build(): FamilyMember = FamilyMember(
      memberId = memberId,
      role = role,
      profile = profile,
      hohGivenName = hohGivenName,
      tags = tags,
      unknownFields = buildUnknownFields()
    )
  }

  public companion object {
    @JvmField
    public val ADAPTER: ProtoAdapter<FamilyMember> = object : ProtoAdapter<FamilyMember>(
      FieldEncoding.LENGTH_DELIMITED, 
      FamilyMember::class, 
      "type.googleapis.com/google.familymanagement.v1.FamilyMember", 
      PROTO_2, 
      null, 
      "familymanagement.proto"
    ) {
      override fun encodedSize(`value`: FamilyMember): Int {
        var size = value.unknownFields.size
        size += ProtoAdapter.STRING.encodedSizeWithTag(1, value.memberId)
        size += FamilyRole.ADAPTER.encodedSizeWithTag(3, value.role)
        size += MemberProfile.ADAPTER.encodedSizeWithTag(4, value.profile)
        size += ProtoAdapter.STRING.encodedSizeWithTag(6, value.hohGivenName)
        size += ProtoAdapter.INT32.asRepeated().encodedSizeWithTag(7, value.tags)
        return size
      }

      override fun encode(writer: ProtoWriter, `value`: FamilyMember) {
        ProtoAdapter.STRING.encodeWithTag(writer, 1, value.memberId)
        FamilyRole.ADAPTER.encodeWithTag(writer, 3, value.role)
        MemberProfile.ADAPTER.encodeWithTag(writer, 4, value.profile)
        ProtoAdapter.STRING.encodeWithTag(writer, 6, value.hohGivenName)
        ProtoAdapter.INT32.asRepeated().encodeWithTag(writer, 7, value.tags)
        writer.writeBytes(value.unknownFields)
      }

      override fun encode(writer: ReverseProtoWriter, `value`: FamilyMember) {
        writer.writeBytes(value.unknownFields)
        ProtoAdapter.INT32.asRepeated().encodeWithTag(writer, 7, value.tags)
        ProtoAdapter.STRING.encodeWithTag(writer, 6, value.hohGivenName)
        MemberProfile.ADAPTER.encodeWithTag(writer, 4, value.profile)
        FamilyRole.ADAPTER.encodeWithTag(writer, 3, value.role)
        ProtoAdapter.STRING.encodeWithTag(writer, 1, value.memberId)
      }

      override fun decode(reader: ProtoReader): FamilyMember {
        var memberId: String? = null
        var role: FamilyRole? = null
        var profile: MemberProfile? = null
        var hohGivenName: String? = null
        val tags = mutableListOf<Int>()
        val unknownFields = reader.forEachTag { tag ->
          when (tag) {
            1 -> memberId = ProtoAdapter.STRING.decode(reader)
            3 -> try {
              role = FamilyRole.ADAPTER.decode(reader)
            } catch (e: ProtoAdapter.EnumConstantNotFoundException) {
              reader.addUnknownField(tag, FieldEncoding.VARINT, e.value.toLong())
            }
            4 -> profile = MemberProfile.ADAPTER.decode(reader)
            6 -> hohGivenName = ProtoAdapter.STRING.decode(reader)
            7 -> tags.add(ProtoAdapter.INT32.decode(reader))
            else -> reader.readUnknownField(tag)
          }
        }
        return FamilyMember(
          memberId = memberId,
          role = role,
          profile = profile,
          hohGivenName = hohGivenName,
          tags = tags,
          unknownFields = unknownFields
        )
      }

      override fun redact(`value`: FamilyMember): FamilyMember = value.copy(
        profile = value.profile?.let(MemberProfile.ADAPTER::redact),
        unknownFields = ByteString.EMPTY
      )
    }

    private const val serialVersionUID: Long = 0L

    @JvmSynthetic
    public inline fun build(body: Builder.() -> Unit): FamilyMember = Builder().apply(body).build()
  }
}
